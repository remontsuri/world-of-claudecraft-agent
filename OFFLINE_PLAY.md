# Офлайн-игра и путь мухи к полноценной игре

Ответ на вопрос «когда муха будет полноценно играть» — с числами, а не лозунгами.
Всё измерено в реальном симе игры (тот же `src/sim`, что в браузерном Play Offline).

---

## 1. Как запускается офлайн-игра (из README самой игры)

Репозиторий игры описывает **три хоста одного и того же сима**:

| Хост | Что это | Команда |
|---|---|---|
| авторитетный сервер | живой мир worldofclaudecraft.com | `docker compose up -d --build` |
| **офлайн-браузер** | локальный синглплеер, без аккаунта и сервера | `pnpm run dev` → http://localhost:5173 → **Play Offline** |
| headless RL env | тот же сим, но по Gym-интерфейсу для агента | `wow_env.WoWClassicEnv` поверх `dist-env/env_server.cjs` |

```bash
# офлайн-браузер (читаем мир глазами; нужен только для наблюдения)
npm install -g pnpm@10.34.5
pnpm install --frozen-lockfile
pnpm run dev           # открыть http://localhost:5173 → Play Offline

# headless-сим (то, чем управляет муха; детерминирован по сиду)
npx esbuild headless/env_server.ts --bundle --platform=node --format=cjs --outfile=dist-env/env_server.cjs

# кто и куда идёт: Eastbrook Vale (1–7) → Mirefen Marsh (6–13) → Thornpeak Heights (13–20),
# плюс Farshore, Veiled Hollow, Drakelands. Мир фиксирован в src/sim/world_seed.ts.
```

Практический вывод: **обучать и мерить надо на headless-симе** (он детерминирован, без
рендера и сети), а браузерный офлайн — чтобы посмотреть глазами. Сим один и тот же,
так что поведение переносится.

---

## 2. Что муха делает сейчас (полный эпизод, 8000 шагов = 33 игровых минуты)

Замер `progress_eval.py` и полный прогон `train.py --max-steps 8000`:

| Условие | reward | уровень | xp | до 2-го ур. | киллы | смерти | квесты |
|---|---:|---:|---:|---:|---:|---:|---:|
| fly-sampled (обученная, 3 сида) | 39.47 / 38.76 / 39.32 | **1** | **60** | 340 | **0** | 0 | **1** |
| fly greedй (argmax) | 8.00 | 1 | 0 | 400 | 0 | 0 | 0 |

Разбор одного эпизода: первый квест закрыт на **шаге 724**, после чего ~6800 шагов
эпизода уходят в никуда. Прогресса нет: **уровень 1 как в начале, так и в конце**.

Распределение действий (5 эпизодов, 2084 шага, 61 действие использовано):

| Категория | Доля шагов |
|---|---:|
| способности (`ability_1..48`) | **71.6 %** |
| движение (`forward/back/turn/strafe/jump`) | 17.0 % |
| бой (`target_nearest`, `attack`) | 5.6 % |
| взаимодействие (`interact`) | 1.9 % |
| еда/питьё | 1.6 % |
| стой/ничего | 2.2 % |

Топ-действие — всего 10 % шагов: политика дрожит, а не действует. 48 действий-способностей
— это ресурс **один каст за GCD**, значит ~70 % эпизода тратится впустую.

Итог: **сейчас муха не играет.** Она выживает (0 смертей), случайно закрывает стартовый
квест и дальше бродит, кастуя способности. Reward этого не показывает (`questProgress`
капает), а прогресс показывает сразу.

---

## 3. Почему прогресса нет: три конкретные причины (с доказательствами)

1. **Энкодер выбрасывал навигацию.** В obs игры есть позиция игрока (`obs[4]`, `obs[5]`),
   векторы мобов `5 × [dist, sin, cos, hp, level, aggro]` (121…150) и блок интерактива
   `[exists, d/40, sin, cos, type]` (151…155). v1 оставлял от этого одну скалярную
   дистанцию и *среднюю* агрессию — ни направления, ни позиции. С таким входом нельзя ни
   подойти к трупу, ни найти квестодателя.
2. **Квестовый блок — только состояние и прогресс**, без координат цели
   (`obs[156..603]` = 224 × `[state, progress]`, где `state`: 0 не взят, 0.33 активен,
   0.66 сдан, 1 закрыт). Значит «идти к цели квеста» в принципе невыразимо через obs —
   нужен внешний «оракул квестов» из `src/sim/data` (QUEST_ORDER/QUESTS) как доп. канал.
3. **Бюджет обучения — 76 800 шагов** (600 × 128). Эпизод длится 8000 шагов, до 2-го
   уровня нужно 400 xp, до 20-го — 167 200 xp. На таком бюджете длинно-горизонтная
   прогрессия не выучивается.

Плюс γ = 0.99 при шаге 128 и эпизоде 8000: горизонт кредита ~в 6 раз короче эпизода.

---

## 4. Что уже сделано в этом коммите (инструменты, а не обещания)

| Файл | Что даёт |
|---|---|
| `progress_eval.py` | меряет **прогресс**, а не reward: уровень, xp, путь по миру, киллы, квесты, шаг первого квеста, распределение действий; выдаёт вердикт по приёмке M1–M4. Воспроизводит эталон 23.688 на сиде 900001 — сверка с закоммиченным бенчмарком |
| `fly_brain.extract_features_v2` | навигационный энкодер: те же 13 каналов (тот же `circuit.json`, переобучается только readout), но вместо средних — пеленги мобов, близость интерактива и «есть что сдавать» по квестам. Переключается `FLY_FEATURES=v2` |
| `train.py --mask-abilities` | маскирует способности, пока тикает GCD (убирает те 71.6 % шагов), согласованно в роллауте и в PPO-апдейте; обучение и оценка — одним и тем же флагом |

Проверено: v1-путь не изменился (эталон 23.688 воспроизводится, обучение без флага работает),
v2-энкодер и маскированное обучение запускаются (`--updates 5` → чекпойнт сохранён).

---

## 5. Дорожная карта: что считаем «полноценной игрой» и что для этого нужно

Пороги приёмки зашиты в `progress_eval.py` (`ACCEPTANCE`).

| Веха | Критерий (среднее по 3 сидам, полный эпизод 8000) | Что нужно сделать | Оценка бюджета |
|---|---|---|---|
| **M0** ✅ сейчас | выжить эпизод, 1 квест | — | готово |
| **M1** «первый прогресс» | **уровень ≥ 2** (400 xp) | v2-энкодер + маскирование + дольше учить | ~2–5 млн шагов (1–2 ч при 8 env) |
| **M2** «боевой цикл» | ≥ 10 киллов, ≤ 1 смерти, лут (`interact`) | награда за урон/киллы, `interact_prox` уже в v2 | ~5–10 млн шагов (3–6 ч) |
| **M3** «квестовый цикл» | ≥ 5 закрытых квестов за эпизод | награда за `questProgress`, γ→0.999, роллаут 256–512 | ~10–30 млн шагов (1–2 суток) |
| **M4** «выход в мир» | Mirefen/уровень ≥ 6, устойчивый маршрут | **оракул квестов** (координаты цели из `src/sim/data`) + новые входные каналы (пересобрать `circuit.json` с большим `CHANNEL_TYPES`) + иерархия «цель → контроллер» | ~50–100 млн шагов + инженерия |
| **M5** «до капа» | уровень 20 (167 200 xp ≈ 2800 стартовых квестов) | масштаб MMO-гринда | недели; это цель-ориентир, не обещание |

Готовые команды (на твоей машине, где есть GPU/больше ядер):

```bash
cd D:/world-of-claudecraft/fly-woc
# M1: обучение с навигационным энкодером и без каст-спама
FLY_FEATURES=v2 WOC_PYTHON_PATH="D:/woc-game/python" python train.py \
  --policy fly --mask-abilities --updates 20000 --steps 128 --envs 8 \
  --max-steps 8000 --seed 20260915 --no-bench \
  --rewards '{"xp": 0.02, "kill": 1.0, "damageDealt": 0.002, "timePenalty": 0.001, "questProgress": 1.0, "questDone": 10, "levelUp": 5}'
# замер прогресса (обязательно с тем же энкодером и маской)
FLY_FEATURES=v2 WOC_PYTHON_PATH="D:/woc-game/python" python progress_eval.py \
  --policy fly-sampled --episodes 3 --max-steps 8000 --mask-abilities
```

---

## 6. Честные границы

- Пороги M1–M4 — инженерные, не опубликованные чьи-то результаты. В опенсорсе у похожих
  проектов достижения скромнее: у doomfly «learned survival has not been demonstrated»,
  у fly-craftax рост reward при падении выживаемости, у FLYT3 MLP-контроль обошёл коннектом
  (85.7 % против 60.8 %). Никто из них не показывает MMO-прогрессию 1→20.
- Проверено здесь: инференс и инструменты. Обучение до M1+ требует часов и ядер, которых
  в песочнице нет (2 vCPU, ~110 шагов/с на энкодере v1, ~2 с на апдейт 128 шагов).
- `random`-baseline невоспроизводим (RNG `action_space` не сидится `reset()`), поэтому в
  отчётах он не используется как ориентир прогресса.
