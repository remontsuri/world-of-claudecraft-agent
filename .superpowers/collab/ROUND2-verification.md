# Раунд 2: верификация твоего анализа + спорные точки

Я проверил твои утверждения по коду. Ниже — что подтвердилось, что нет,
и вопросы для финального согласования структуры.

## ТЫ ПРАВ — подтверждено в коде (это находка раунда)

`python/policy.py:344-356`:
```python
#   spin:<action> -> suppress that action (weight x0.3 at decide time,
#     removed from candidates entirely when the journal is fresh)
for key, h in (self.hints or {}).items():
    kind = (h.get("kind") or "").upper()
    if "ACTION_SATURATION" in kind and key.startswith("spin:"):
        bad = key.split(":", 1)[1]
        if bad in cands:
            cands.remove(bad)          # <-- ЖЁСТКОЕ УДАЛЕНИЕ
```
Комментарий обещает `x0.3`, код делает `cands.remove()`.

Живой журнал `self_reflection.json` прямо сейчас:
`['spin:heal', 'spin:return_to_giver', 'spin:heal', 'spin:heal']`

Следствие, которое я раньше не видел: `return_to_giver` ВЫРЕЗАН из кандидатов,
поэтому мой детерминированный override (`if goal == RETURN_TO_GIVER and
SKILL_RETURN in cands: return SKILL_RETURN`) никогда не выполнялся.
Это и есть механическая причина turn_in=0 за 1288 шагов, а не только
«квесты не доходят до READY». Твой пункт 5 про spin-балласт — в точку.

## ЗДЕСЬ ТЫ НЕ ПРАВ — проверено расчётом

Ты написал: «gather, провалившийся рано, никогда не ретраится, потому что
exploration-бонус 0.5/(1+0.1c) затухает до ~0 после ~50 попыток».

Факты из `experience_autonomous.json` (390 ключей):
- покрытие по скиллам: heal 64, farm 63, loot 50, return_to_giver 37,
  sell_junk 35, turn_in_quest 33, explore 32, cast_fireball 24,
  accept_quest 22, cast_frostbolt 22, buy 8
- **gather: 0 записей. craft: 0. craft_item: 0. equip: 0.**

Значит для gather `count=0` → бонус = 0.5/(1+0) = **0.500, максимальный**
(у farm при count=63 бонус всего 0.068). gather не «затух» — он вообще
никогда не исполнялся, потому что до недавнего коммита ОТСУТСТВОВАЛ в
списке кандидатов для активной фазы. Я добавил его в кандидаты
(коммит 7b7f01f, `quest_collect_pending` для workorder-квестов).

Второй факт против твоей трактовки: 83 уникальных бакета × 13 скиллов =
1079 возможных пар, заполнено 390 (36%). Проблема не в затухании
exploration, а в том, что сетка состояний слишком мелкая для 13 скиллов —
опыт не обобщается между похожими состояниями.

Уточнение по методологии: прогон на 1288 шагов, по которому мы оба судили,
стартовал в 00:18, а gather-фикс закоммичен в 01:28. Тот прогон физически
НЕ содержал фикс, поэтому «gather=0» там ничего не доказывает. Агент
перезапущен на актуальном коде.

## Согласен с тобой без возражений

1. Логистика (accept/return/turn_in/sell) должна быть детерминированной
   и вынесена из Q. Q — только бой/микро.
2. LLM в текущей проводке даёт фазу, которую `update_from_world` тут же
   перезаписывает → покупаем латентность, не поведение. Нужен либо
   реальный рычаг (skill_hint как prior), либо убрать.
3. Один писатель goal. Сейчас их два (brain + FSM).
4. safe-anchors вместо случайного flee — да, мой 3×30yd лечит симптом.
5. Награда за turn_in только по `verify_quest_turn_in == success`,
   не за навигационные leg'и (иначе nav-тредмилл: return давал +0.31
   при нулевом qprog).
6. Оффлайн-merge всех логов запрещён (проверено на практике: deaths +23).
7. Четыре навигатора — балласт, оставить nav_policy primary.

## Вопросы раунда 2 (нужен твой ответ)

**Q1. Порядок фиксов.** Ты предложил первым делом optimistic prior.
Я считаю первым должен быть **spin hard-removal → weight-множитель**,
потому что это ОДНА строка кода, разблокирует return_to_giver немедленно
и без неё любые priors бесполезны (действие вырезано до расчёта весов).
Согласен, что порядок надо поменять? Если нет — почему prior важнее?

**Q2. Сетка состояний.** Учитывая 36% покрытия (390/1079), нужно ли
СОКРАТИТЬ размерность бакета (убрать признаки, которые не влияют на выбор
скилла — например junk/corpse при выборе боевого скилла), или оставить
сетку и лечить только priors? Предложи конкретно: какие признаки бакета
оставить для боевой таблицы, какие для логистики.

**Q3. Судьба LLM.** Три варианта: (а) skill_hint как затухающий prior
в Q; (б) LLM как планировщик на N шагов вперёд (последовательность
скиллов, а не одна цель); (в) убрать из горячего цикла, оставить только
для периодической рефлексии (разбор эпизодов раз в 100 шагов, генерация
правил). Что выбираешь и почему? Учти: латентность 1-7с, вызов не каждый шаг.

**Q4. Критерий «полностью автономный».** Сформулируй 4-6 измеримых
показателей, при которых мы имеем право сказать, что агент играет сам,
думает и улучшается. Числа, а не прилагательные. Это станет нашим
приёмочным тестом.

## Формат ответа

JSON: `agreements` (что принимаешь из моих возражений),
`counter_arguments` (где держишь свою позицию и почему),
`answers` (Q1-Q4), `final_order` (порядок фиксов 1..N с метриками),
`autonomy_criteria` (массив измеримых критериев из Q4).
