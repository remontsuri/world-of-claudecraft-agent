# Задача C: проверить мои допущения по ИСХОДНИКАМ игры

Ты — верификатор. Я (ведущий) сделал серию фиксов, опираясь на ДОГАДКИ о
правилах игры. Нужно проверить каждое допущение по реальному коду симуляции
и сказать, где я ошибся.

Исходники игры: `D:\world-of-claudecraft\src\sim\` (TypeScript, авторитетный
сим). Ключевые файлы: `interaction.ts`, `sim.ts`, `quests.ts` (если есть),
`combat/`, `content/zone1.ts`, `obs.ts`, `casting_lifecycle.ts`.

## Контекст: что наблюдается в игре

Агент 67 шагов простоял в фазе TURN_IN. Вызовы:
```
accept_quest:    7  -> ВСЕ 7 inconclusive
turn_in_quest:   7  -> ВСЕ 7 INCONCLUSIVE
return_to_giver: 0
```
Гиверы были в 59-65 yd. Квест `q_prof_workorder_loom` готов (6/6).

## МОИ ДОПУЩЕНИЯ — проверь каждое (verdict: TRUE / FALSE / PARTIAL)

**C1.** `INTERACT_RANGE = 7.0` ярдов — дистанция, в пределах которой сервер
принимает сдачу квеста и разговор с NPC. Я захардкодил 7.0 в
`python/quest_truth.py`. Найди РЕАЛЬНОЕ значение в исходниках (искать:
`INTERACT_RANGE`, `interactRange`, `INTERACT_DIST`, проверки дистанции в
`interaction.ts`). Назови файл:строку и число.

**C2.** Сдача квеста (`turnInQuest` / аналог) требует: квест в состоянии
ready/complete + игрок в пределах INTERACT_RANGE от turn-in NPC + свободное
место в сумках. Найди ФАКТИЧЕСКИЙ список проверок в коде и порядок отказов.
Какие ещё условия есть, которых я не учёл? Что возвращает функция при отказе
(бросает? пишет ошибку в события? тихо выходит)?

**C3.** Взятие квеста (`acceptQuest` / аналог) требует: NPC рядом (какая
дистанция?) + квест не взят + выполнены prerequisites (minLevel? цепочка
предыдущих квестов? профессия?). У NPC рядом были квесты
`q_prof_attune_outfitter`, `q_prof_amends_outfitter`,
`q_prof_attune_bombardier`, `q_prof_amends_bombardier` — проверь в
`content/zone1.ts`, какие у них требования (minLevel, требуемая профессия,
предшествующий квест). Может, агент физически не может их взять?

**C4.** Атака требует, чтобы персонаж СМОТРЕЛ на цель. Я добавил доворот
через `controller.face()` перед кастом и перед авто-атакой, порог 0.12 рад.
Проверь: есть ли в коде боя (`combat/`, `casting_lifecycle.ts`) реальная
проверка направления/угла? Или урон наносится независимо от facing, а
доворот — только визуальная деталь? Назови файл:строку.

**C5.** `controller.face(angle)` — правильный способ задать курс. Найди эту
функцию (вероятно в `src/main.ts` или контроллере), проверь: она ставит
facing мгновенно, интерполирует, или ставит целевой курс для плавного
поворота? Есть ли побочные эффекты (сброс движения, отмена каста)?

**C6.** `spider_silk` добывается харвестом трупа с componentTag `silk` через
`sim.harvestCorpse(mobId, tags)`. Проверь в `content/professions.ts` и
`interaction.ts`: какие условия у harvestCorpse (нужен ли инструмент? тир?
не разграблен ли труп? кто может харвестить?).

**C7.** Состояния квеста в снапшоте: `active` / `ready` / `done`. Проверь в
исходниках, какие состояния РЕАЛЬНО существуют (может быть `complete`,
`failed`, `abandoned`) и как они попадают в снапшот моста
(`src/bridge/snapshot.cjs`). Моя логика в `python/quest_truth.py` считает
фазу по трём ведрам — этого достаточно?

## Ограничения

- **НИЧЕГО НЕ ЗАПУСКАЙ**: ни агента (`play_autonomous`), ни мост
  (`browser_bridge.cjs`). Они сейчас РАБОТАЮТ, их нельзя трогать.
- Код НЕ меняй. Только чтение исходников.
- Для поиска используй search_files / grep по `D:\world-of-claudecraft\src\`.
- Если файла нет — скажи прямо, не выдумывай.

## Формат ответа

JSON: массив `findings`, по одному объекту на C1-C7:
`{id, my_assumption, verdict: TRUE|FALSE|PARTIAL, evidence (файл:строка +
цитата), correct_value_or_rule}`.
Плюс `wrong_fixes` — список моих фиксов, которые надо переделать, и
`missing_conditions` — условия игры, которые я вообще не учёл.
