# LLM Brain Implementation Plan (ExecPlan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Локальная Qwen3.6-35B (:8081) становится стратегическим мозгом агента — выбирает
ЦЕЛЬ из enum'а на переходах; Q-learning остаётся тактическим исполнителем.

**Architecture:** Новый модуль `python/llm_brain.py` (HTTP к llama-server, json_schema
strict, таймаут-фоллбек) + эпизодическая память `python/episodic_log.jsonl` + три точки
интеграции в `play_autonomous.py` (consult/apply/log) под env-переключателем `WOC_BRAIN`.
Bridge/skills/sim не изменяются. Отказ LLM = текущее поведение FSM+Q (нулевая деградация).

**Tech Stack:** Python 3.12 (urllib, без новых зависимостей), llama.cpp OpenAI-compatible
API (`response_format: json_schema`, strict), существующие SelfReflection + WorldMemory.

**Spec:** `docs/superpowers/specs/2026-08-23-llm-brain-design.md`

## Global Constraints

- Python runner: `"C:/Users/vladc/AppData/Local/Programs/Python/Python312/python.exe"`,
  всегда с пустым `PYTHONPATH` (Hermes venv leak).
- Никаких правок `src/**` (game source), bridge (`browser_bridge.cjs`, `src/bridge/*`)
  и sim. Только `python/` + `docs/`.
- TDD: каждый компонент начинается с падающего теста (pytest, стиль существующих
  `test_*.py` в `python/`). Мок HTTP — без сети.
- Коммит после каждой зелёной задачи; push `git push mine backup`.
- Цели enum'а ровно: ACCEPT, DO_OBJECTIVE, RETURN_TO_GIVER, TURN_IN, SELL_REPAIR,
  HEAL, SURVIVE.
- LLM-вызовы только когда `should_consult()` истинно; любой сбой → None → поведение
  идентично WOC_BRAIN=off.

---

### Task 1: Episodic memory writer/reader

**Files:**
- Create: `python/episodic.py`
- Test: `python/test_episodic.py`

**Interfaces:**
- Produces:
  - `class EpisodicLog: __init__(path=None); append(rec: dict) -> None;
     recent(quest_id: str | None = None, n: int = 5) -> list[dict];
     recent_failures(n: int = 3) -> list[dict]`
  - Формат строки jsonl: `{"t","quest","step","action","result","reason","hp_frac","phase"}`
  - `append` толерантен: отсутствующие ключи → null; файл отсутствует → создаётся.

- [ ] **Step 1: Write failing tests**

```python
# python/test_episodic.py
import os, sys, tempfile
sys.path.insert(0, os.path.dirname(__file__))
from episodic import EpisodicLog

def test_append_and_recent_by_quest():
    td = tempfile.mkdtemp()
    log = EpisodicLog(path=os.path.join(td, "e.jsonl"))
    log.append({"t": 1, "quest": "q_a", "step": 1, "action": "accept_quest",
                "result": "FAILURE", "reason": "already_active", "hp_frac": 0.9,
                "phase": "NO_QUEST"})
    log.append({"t": 2, "quest": "q_b", "step": 2, "action": "farm",
                "result": "SUCCESS", "hp_frac": 0.9})
    assert [r["quest"] for r in log.recent("q_a", n=5)] == ["q_a"]
    assert len(log.recent(None, n=10)) == 2

def test_recent_failures_only_failures():
    td = tempfile.mkdtemp()
    log = EpisodicLog(path=os.path.join(td, "e.jsonl"))
    for i, res in enumerate(["SUCCESS", "FAILURE", "SUCCESS", "FAILURE"]):
        log.append({"t": i, "quest": f"q{i}", "step": i, "action": "x",
                    "result": res, "hp_frac": 0.9})
    fails = log.recent_failures(n=3)
    assert len(fails) == 2 and all(f["result"] == "FAILURE" for f in fails)

def test_missing_fields_tolerated():
    td = tempfile.mkdtemp()
    log = EpisodicLog(path=os.path.join(td, "e.jsonl"))
    log.append({"t": 1, "action": "loot"})          # почти пусто
    r = log.recent(n=1)[0]
    assert r["result"] is None and r["quest"] is None

def test_corrupt_lines_skipped():
    td = tempfile.mkdtemp()
    p = os.path.join(td, "e.jsonl")
    with open(p, "w", encoding="utf-8") as f:
        f.write('{"t":1,"quest":"q_x","action":"a"}\nNOT JSON AT ALL\n')
    log = EpisodicLog(path=p)
    assert len(log.recent(n=10)) == 1
```

- [ ] **Step 2: Run tests, verify FAIL** —
  `cd python && PYTHONPATH="" "C:/Users/vladc/AppData/Local/Programs/Python/Python312/python.exe" -m pytest test_episodic.py -q`
  Expected: ImportError (episodic не существует).

- [ ] **Step 3: Implement episodic.py**

```python
"""Episodic memory: one jsonl line per attempt (the agent's own experience)."""
import json, os, threading

_LOCK = threading.Lock()

def _norm(rec: dict) -> dict:
    return {
        "t": rec.get("t"), "quest": rec.get("quest"), "step": rec.get("step"),
        "action": rec.get("action"), "result": rec.get("result"),
        "reason": rec.get("reason"), "hp_frac": rec.get("hp_frac"),
        "phase": rec.get("phase"),
    }

class EpisodicLog:
    def __init__(self, path=None):
        self.path = path or os.path.join(
            os.path.dirname(os.path.abspath(__file__)), "episodic_log.jsonl")

    def append(self, rec: dict):
        line = json.dumps(_norm(rec), ensure_ascii=False)
        with _LOCK:
            with open(self.path, "a", encoding="utf-8") as f:
                f.write(line + "\n")

    def _load(self):
        if not os.path.exists(self.path):
            return []
        out = []
        with open(self.path, encoding="utf-8") as f:
            for line in f:
                line = line.strip()
                if not line.startswith("{"):
                    continue
                try:
                    out.append(json.loads(line))
                except Exception:
                    continue
        return out

    def recent(self, quest_id=None, n=5):
        rows = self._load()
        if quest_id is not None:
            rows = [r for r in rows if r.get("quest") == quest_id]
        return rows[-n:]

    def recent_failures(self, n=3):
        rows = [r for r in self._load() if r.get("result") == "FAILURE"]
        return rows[-n:]
```

- [ ] **Step 4: Tests PASS** (та же команда). Expected: 4 passed.
- [ ] **Step 5: Commit**
```bash
git add python/episodic.py python/test_episodic.py
git commit -m "feat(memory): episodic attempt log (jsonl) for LLM brain"
git push mine backup
```

---

### Task 2: LLMBrain — HTTP клиент со strict-schema и фоллбеком

**Files:**
- Create: `python/llm_brain.py`
- Test: `python/test_llm_brain.py`

**Interfaces:**
- Consumes: urllib (stdlib).
- Produces:
  - `GOALS = ("ACCEPT","DO_OBJECTIVE","RETURN_TO_GIVER","TURN_IN","SELL_REPAIR","HEAL","SURVIVE")`
  - `class LLMBrain: __init__(base_url="http://127.0.0.1:8081", timeout=20.0)
     decide(world: dict, failures: list[dict], lessons: list[str]) -> dict | None`
  - Возвращает `{"goal": <enum>, "reason": <str>}` либо `None` (сбой/мусор/таймаут).

- [ ] **Step 1: Failing tests**

```python
# python/test_llm_brain.py
import os, sys, json, urllib.request
sys.path.insert(0, os.path.dirname(__file__))
import llm_brain
from llm_brain import LLMBrain

class FakeResp:
    def __init__(self, payload): self._p = json.dumps(payload).encode()
    def read(self): return self._p
    def __enter__(self): return self
    def __exit__(self, *a): return False

def _ok_content(goal="DO_OBJECTIVE", reason="r"):
    return {"choices":[{"message":{"content":json.dumps({"goal":goal,"reason":reason})}}]}

def test_decide_parses_valid(monkeypatch=None):
    b = LLMBrain(base_url="http://mock")
    captured = {}
    def fake_urlopen(req, timeout):
        captured["body"] = json.loads(req.data.decode())
        return FakeResp(_ok_content("TURN_IN", "ready and near"))
    llm_brain.urllib.request.urlopen = fake_urlopen
    out = b.decide({"quest": {"phase":"READY"}}, [], [])
    assert out == {"goal": "TURN_IN", "reason": "ready and near"}
    rf = captured["body"]["response_format"]
    assert rf["type"] == "json_schema"
    enum = rf["json_schema"]["schema"]["properties"]["goal"]["enum"]
    assert set(enum) == set(llm_brain.GOALS)
    assert captured["body"]["temperature"] == 0.0

def test_invalid_goal_returns_none():
    b = LLMBrain(base_url="http://mock")
    llm_brain.urllib.request.urlopen = lambda req, timeout: FakeResp(
        _ok_content("CONQUER_WORLD"))
    assert b.decide({}, [], []) is None

def test_garbage_json_returns_none():
    b = LLMBrain(base_url="http://mock")
    llm_brain.urllib.request.urlopen = lambda req, timeout: FakeResp(
        {"choices":[{"message":{"content":"не JSON вообще"}}]})
    assert b.decide({}, [], []) is None

def test_timeout_returns_none():
    import urllib.error
    b = LLMBrain(base_url="http://mock", timeout=0.05)
    def slow(req, timeout): raise TimeoutError("too slow")
    llm_brain.urllib.request.urlopen = slow
    assert b.decide({}, [], []) is None

def test_prompt_contains_world_and_failures():
    b = LLMBrain(base_url="http://mock")
    seen = {}
    def fake(req, timeout):
        seen["body"] = json.loads(req.data.decode()); return FakeResp(_ok_content())
    llm_brain.urllib.request.urlopen = fake
    b.decide({"quest": {"id": "q_bones", "phase": "ACTIVE"}},
             [{"action": "turn_in_quest", "result": "FAILURE",
               "reason": "wrong_npc"}],
             ["IF phase==ACTIVE THEN never ACCEPT"])
    user_msg = seen["body"]["messages"][-1]["content"]
    assert "q_bones" in user_msg and "wrong_npc" in user_msg and "never ACCEPT" in user_msg
```

- [ ] **Step 2: Verify FAIL** (ImportError).
- [ ] **Step 3: Implement llm_brain.py**

```python
"""LLM strategic brain: picks a GOAL from a fixed enum via local llama-server."""
import json
import urllib.request

GOALS = ("ACCEPT", "DO_OBJECTIVE", "RETURN_TO_GIVER",
         "TURN_IN", "SELL_REPAIR", "HEAL", "SURVIVE")

_SCHEMA = {
    "name": "agent_decision", "strict": True,
    "schema": {
        "type": "object",
        "properties": {
            "goal": {"type": "string", "enum": list(GOALS)},
            "reason": {"type": "string"},
        },
        "required": ["goal"], "additionalProperties": False,
    },
}

_SYSTEM = (
    "Стратегический мозг MMO-агента World of ClaudeCraft. Правила выбора цели:\n"
    "- ACCEPT: только квест, которого НЕТ в логе (phase=AVAILABLE)\n"
    "- DO_OBJECTIVE: phase=ACTIVE и progress<required\n"
    "- RETURN_TO_GIVER: progress>=required, но giver далеко (>7yd)\n"
    "- TURN_IN: phase=READY И giver рядом (<7yd)\n"
    "- HEAL: hp_frac<0.35; SURVIVE: dead или hp_frac<0.15\n"
    "- SELL_REPAIR: сумки полны и рядом vendor\n"
    "Учитывай recent_failures: не повторяй недавние проваленные действия.\n"
    "Отвечай только JSON по схеме."
)


class LLMBrain:
    def __init__(self, base_url: str = "http://127.0.0.1:8081", timeout: float = 20.0):
        self.base_url = base_url.rstrip("/")
        self.timeout = timeout

    def decide(self, world: dict, failures: list, lessons: list):
        body = {
            "messages": [
                {"role": "system", "content": _SYSTEM},
                {"role": "user", "content": json.dumps(
                    {"world": world, "recent_failures": failures,
                     "lessons": lessons}, ensure_ascii=False)},
            ],
            "temperature": 0.0,
            "max_tokens": 120,
            "response_format": {"type": "json_schema", "json_schema": _SCHEMA},
        }
        req = urllib.request.Request(
            self.base_url + "/v1/chat/completions",
            data=json.dumps(body, ensure_ascii=False).encode("utf-8"),
            headers={"content-type": "application/json"})
        try:
            with urllib.request.urlopen(req, timeout=self.timeout) as resp:
                data = json.loads(resp.read())
            content = data["choices"][0]["message"]["content"]
            parsed = json.loads(content)
            goal = parsed.get("goal")
            if goal not in GOALS:
                return None
            return {"goal": goal, "reason": str(parsed.get("reason", ""))}
        except Exception:
            return None
```

- [ ] **Step 4: Tests PASS.**
- [ ] **Step 5: Live smoke (не unit — ручная проверка на реальном :8081)**

```bash
PYTHONPATH="" python -c "
from llm_brain import LLMBrain
b = LLMBrain()
print(b.decide({'quest': {'id':'q_bones','phase':'ACTIVE','progress':6,'required':10},
                'giver_distance':83,'player':{'hp_frac':0.71}},
               [{'action':'accept_quest','result':'FAILURE','reason':'already_active'}], []))"
```
Expected: `{'goal': 'DO_OBJECTIVE', 'reason': ...}` (проверено в спайке).

- [ ] **Step 6: Commit + push** (`feat(brain): LLM strategic decision client, strict schema`).

---

### Task 3: should_consult — политика вызовов

**Files:**
- Modify: `python/llm_brain.py`
- Test: `python/test_llm_brain_consult.py`

**Interfaces:**
- Produces: `LLMBrain.should_consult(goal_prev, goal_new, step_idx, consecutive_failures,
  new_quest: bool) -> bool`. Правила: смена цели ИЛИ new_quest ИЛИ failures>=3 ИЛИ
  step_idx % 50 == 0. Первое решение (goal_prev is None) → True.

- [ ] **Step 1: Failing tests**

```python
# python/test_llm_brain_consult.py
import os, sys
sys.path.insert(0, os.path.dirname(__file__))
from llm_brain import LLMBrain

b = LLMBrain()

def test_first_decision_always():
    assert b.should_consult(None, "DO_OBJECTIVE", 7, 0, False) is True

def test_phase_change_consults():
    assert b.should_consult("DO_OBJECTIVE", "TURN_IN", 41, 0, False) is True

def test_same_goal_no_consult():
    assert b.should_consult("DO_OBJECTIVE", "DO_OBJECTIVE", 41, 0, False) is False

def test_three_failures_consult():
    assert b.should_consult("DO_OBJECTIVE", "DO_OBJECTIVE", 43, 3, False) is True

def test_new_quest_consults():
    assert b.should_consult("DO_OBJECTIVE", "DO_OBJECTIVE", 44, 0, True) is True

def test_periodic_consult_every_50():
    assert b.should_consult("DO_OBJECTIVE", "DO_OBJECTIVE", 50, 0, False) is True
    assert b.should_consult("DO_OBJECTIVE", "DO_OBJECTIVE", 51, 0, False) is False
```

- [ ] **Step 2: Verify FAIL** (AttributeError).
- [ ] **Step 3: Implement**

```python
    def should_consult(self, goal_prev, goal_new, step_idx,
                       consecutive_failures, new_quest):
        if goal_prev is None or goal_new != goal_prev or new_quest:
            return True
        if consecutive_failures >= 3:
            return True
        return step_idx % 50 == 0
```

- [ ] **Step 4: PASS.** - [ ] **Step 5: Commit+push** (`feat(brain): consult policy`).

---

### Task 4: Интеграция в play_autonomous (WOC_BRAIN switch)

**Files:**
- Modify: `python/play_autonomous.py` (init-блок рядом с `refl = SelfReflection()`,
  цикл после `goal_fsm.update_from_world(ws)`, запись эпизода после `row = {...}`)
- Test: `python/test_brain_integration.py`

**Interfaces:**
- Consumes: Task 1–3; `goal_fsm`; `rec` (шаговый словарь); `ws` (WorldState).
- Produces: env `WOC_BRAIN=on` активирует мозг; `[brain]` лог-строки; файл
  `episodic_log.jsonl` заполняется при каждом шаге (независимо от переключателя).

- [ ] **Step 1: Failing integration test** — проверяем ЧИСТУЮ функцию сборки
  контекста и применения решения (без реального HTTP):

```python
# python/test_brain_integration.py
import os, sys
sys.path.insert(0, os.path.dirname(__file__))

def test_build_world_payload_shape():
    from play_autonomous import build_brain_payload
    ws = {"quest": {"id": "q_bones", "phase": "ACTIVE", "progress": 6,
                    "required": 10, "giver_distance": 83.0},
          "hp_frac": 0.71, "danger": False}
    info = {"player_pos": [1, 2]}
    fsm_qid = "q_bones"
    p = build_brain_payload(ws, info, fsm_qid)
    assert p["quest"]["id"] == "q_bones"
    assert p["giver_distance"] == 83.0
    assert p["player"]["hp_frac"] == 0.71
    assert "dead" in p["player"]

def test_apply_decision_sets_fsm(tmp_path=None):
    import tempfile
    from goal_fsm import GoalFSM, DO_OBJECTIVE
    f = GoalFSM(path=tempfile.mkdtemp() + "/g.json")
    f.set("TURN_IN", "q_old")
    from play_autonomous import apply_decision
    applied = apply_decision(f, {"goal": "DO_OBJECTIVE", "reason": "active 6/10"})
    assert applied and f.goal == DO_OBJECTIVE and f.quest_id == "q_old"

def test_apply_rejects_none():
    from goal_fsm import GoalFSM
    f = GoalFSM(path=tempfile.mkdtemp() + "/g.json")
    f.set("TURN_IN", "q_old")
    assert apply_decision(f, None) is False
    assert f.goal == "TURN_IN"
```

- [ ] **Step 2: Verify FAIL.**
- [ ] **Step 3: Implement в play_autonomous.py**

```python
def build_brain_payload(ws, info, fsm_quest_id):
    q = dict(ws.get("quest") or {})
    q["id"] = fsm_quest_id or q.get("id")
    return {
        "quest": q,
        "giver_distance": ws.get("distance_to_giver"),
        "player": {"hp_frac": ws.get("hp_frac"),
                   "dead": bool((info.get("player") or {}).get("dead"))},
        "bag_slots": len([s for s in (info.get("inventory") or []) if s]),
    }

def apply_decision(fsm, decision):
    """Apply an LLM {goal,reason} to the FSM. Returns True if applied."""
    if not isinstance(decision, dict) or decision.get("goal") not in (
            "ACCEPT","DO_OBJECTIVE","RETURN_TO_GIVER","TURN_IN",
            "SELL_REPAIR","HEAL","SURVIVE"):
        return False
    g = decision["goal"]
    mapping = {"SURVIVE": "HEAL"}
    g = mapping.get(g, g)
    fsm.set(g, fsm.quest_id)
    return True
```

В `main()` рядом с `refl = SelfReflection()`:

```python
brain_on = os.environ.get("WOC_BRAIN", "off").lower() == "on"
from llm_brain import LLMBrain
brain = LLMBrain() if brain_on else None
episodes = EpisodicLog()   # from episodic import EpisodicLog
brain_last_goal = None
brain_fail_streak = 0
```

В цикле, сразу после `goal_fsm.update_from_world(ws)`:

```python
if brain is not None:
    try:
        if brain.should_consult(brain_last_goal, goal_fsm.goal, i,
                                brain_fail_streak,
                                goal_fsm.quest_id != getattr(build_brain_payload, "_last_qid", None)):
            world = build_brain_payload(ws, info, goal_fsm.quest_id)
            fails = episodes.recent_failures(n=3)
            lessons = [c.get("detail") for c in refl.journal[-5:]]
            d = brain.decide(world, fails, lessons)
            if d and apply_decision(goal_fsm, d):
                print(f"[brain] goal={d['goal']} reason={d['reason']}", flush=True)
                brain_last_goal = d["goal"]
    except Exception:
        traceback.print_exc()
```

После `logf.write(json.dumps(row...)`:

```python
episodes.append({
    "t": time.time(), "quest": goal_fsm.quest_id, "step": i,
    "action": a, "result": rec["verdict"],
    "reason": rec.get("outcome_kind"), "hp_frac": ws.get("hp_frac"),
    "phase": goal_fsm.goal,
})
brain_fail_streak = brain_fail_streak + 1 if rec["verdict"] in ("FAILURE",) else 0
```

- [ ] **Step 4: py_compile + все тесты PASS** (все `test_*.py`).
- [ ] **Step 5: Commit+push** (`feat(brain): wire LLM decisions into autonomous loop (WOC_BRAIN=on)`).

---

### Task 5: Живой A/B прогон (acceptance)

**Files:** нет новых; артефакты `autonomous_log.jsonl`, консоль.

- [ ] **Step 1:** Бридж жив (`curl :8791/health`), llama жив (`curl :8081/health`),
  Chrome с игрой открыт. Запуск OFF:
  `WOC_BRAIN=off` → 3000 шагов → сохранить копию `autonomous_log_off.jsonl` +
  FINAL summary (quests_turned_in, deaths, повторные фейлы).
- [ ] **Step 2:** Запуск ON: `rm -f python/play_autonomous.lock` →
  `WOC_BRAIN=on` те же 3000 шагов → те же метрики.
- [ ] **Step 3:** Сравнение окон по 100 шагов (скрипт `_analyze_runs.py` уже есть).
  Acceptance: on → quests_turned_in > 0 И deaths <= off И меньше дублей
  одинаковых FAILURE подряд. Результат зафиксировать здесь (живой документ).
- [ ] **Step 4:** Итоговый отчёт пользователю + коммит плана с результатами.

## Progress Log (living)

- 2026-08-23: план создан; Tasks 1–4 не начаты; спека закоммичена.
