# woof-ts — активная линия: факты и решения

Здесь живёт знание о линии `woof-ts/` (лёгкий стек: Node 20 + TypeScript + esbuild + vitest).
Записи **датированные и только добавляются**: факт, чем проверен, что из этого следует.
Протоколы и команды — в `woof-ts/BRIDGE.md`, `woof-ts/LEARNING.md`, `woof-ts/README.md`;
состояние на сегодня — в `woof-ts/PROGRESS-<дата>.md`; долг — в `woof-ts/ROADMAP.md`.

## Инварианты линии (не зависят от даты)

* Мир — за интерфейсом `World` (`src/world/world.ts`). Политика не знает, где мир:
  в процессе (`SimWorld`) или за бриджем (`NdjsonBridge`).
* Возможности мира (`capabilities`) объявляются до прогона, печатаются и пишутся в evidence.
  Привилегированный метод без объявленной возможности бросает исключение.
* Факты игры — только импортом из `woof-ts/game` (→ `~/woc-game`, sparse-клон
  `levy-street/world-of-claudecraft`, MIT). Сверка: `tools/verify_facts.ts`, `tests/facts.test.ts`.
* Пороги агента — одна таблица `src/policy/params.ts` (`PARAM_SPECS`, 34 параметра:
  ключ, def, min, max, шаг, группа, пояснение). Загрузка — только `WOOF_PARAMS=<file.json>`.
* Контур улучшения: `src/learn/objective.ts` (цель) + `src/learn/tune.ts` (подбор),
  история `learning/history.jsonl` append-only.
* Один прогон = один процесс. Единицы `world_steps` одинаковы в обоих транспортах.

## 2026-09-18 — перенос линии на лёгкий стек и первый бридж

* **Стек выбран и зафиксирован**: Node 20, TypeScript, esbuild, vitest. Next.js никогда не
  устанавливался и не обсуждался как выбранный стек — операторский веб-слой не нужен,
  потому что приёмка идёт по CLI-прогонам и evidence-файлам. Чем проверено: `package.json`
  (dev-зависимости: `typescript`, `esbuild`, `vitest`, `@types/node`), отсутствие
  `next`/`react` в дереве.
* **Архитектура перенесена из Java-линии, а не переписана с нуля**: тот же контур
  `OBSERVE → DECIDE → EXECUTE → VERIFY → LEARN`, те же роли (память, FSM, арбитраж,
  исполнитель). Порт архитектуры позволил сохранить известные дефекты B1–B8/B13 как
  явный долг вместо того, чтобы заново их находить.
* **Бридж к игре работает**: env-сервер игры собирается **нашим** esbuild из дерева игры
  (`tools/build_env.sh` → `dist-env/env_server.cjs`, 6.8 МБ, ~0.5 с), потому что
  `npm install` в игре тянет весь фронтенд (vite/react), а серверу нужны только
  `src/sim` + `headless`. Дерево игры не модифицируется.
* **Синхронный канал к процессу мира** — воркер + `SharedArrayBuffer` + `Atomics`
  (`src/bridge/sync_channel.ts`): `fs.readSync` на pipe даёт EAGAIN (libuv делает pipe
  неблокирующим), а `Buffer.from(view, off, len)` игнорирует offset/length — нужен
  `subarray`. Замер: 3001 круг за 503 мс (~0.17 мс на круг).
* **Эквивалентность наблюдения доказана на живом мире**: тот же сид и та же последовательность
  из 25 действий дают совпадающие kills/deaths/quests_done, HP, координаты, число и
  дистанции ближайших мобов, готовность и кулдауны способностей (точность 1e-4…1e-6).
  Чем проверено: `tests/bridge_env.test.ts` (15 тестов, живой `env_server.cjs`).
* **Первый критерий «прогон состоялся» выполнен за бриджем**: seed 42, warrior, 120 решений →
  `world_steps=5530 kills=2 deaths=0 quests_done=1 first_turn_in_step=6`. Квест —
  `q_hub_know_your_numbers` у Drillmaster Hale: дойти по пину карты, `ACCEPT`, 10 ударов
  по манекену, `TURN_IN` на шаге 6.
* **Ограничения бриджа объявлены, а не обойдены**: `entityIdentity='slot'`,
  `entityTemplates=false`, `entityAbsoluteHp=false`, `questStateApi='observed'`,
  `damageCounters=false`, `itemCountApi=false`, `questObjectiveCounts=false`.
  Следствия для политики (все — исправления политики, а не подделки данных):
  бой по наблюдаемой Δуровня; успех боя по падению наблюдаемой доли HP цели (`FARM_HP:…`);
  ближайший пин вместо «угадывания» доступности квеста; вычёркивание квестодателя после
  `giverFailExhaust` провалов.
* **Контур самоулучшения построен и продемонстрирован**: цель
  `100·quests_done + 5·kills − 10·deaths + 25·(level−1) + 20·[first_turn_in≤40] − 0.001·world_steps`
  объявлена до измерения; demo-подбор (`--steps 60 --train-seeds 42 --val-seeds 44
  --groups quest`) принял `levelDeltaMax: 1→0` (train −71.56 → 116.52; val −14.02 → −13.95).
* **Находка, важнее самого результата**: скор зависит от бюджета решений. Тот же
  `levelDeltaMax=0` на 150 решениях даёт `kills=0, world_steps=10684, quests_done=1`,
  то есть хуже базового (`24 / 4323 / 2`). Значит подбирать надо на том же `--steps`,
  на котором объявлена приёмка, а короткий бюджет находит «осторожные», а не
  «результативные» настройки. Один val-сид — шум: минимум три.
* **Проверки собраны в один скрипт**: `tools/check_all.sh` (8 шагов: рабочее место → типы →
  факты → тесты → стенд+детерминизм → живость порогов → бридж → контур обучения).
  Типы проверяются по **коду возврата** tsc: фильтр вывода однажды выглядел «чисто»
  при полностью отсутствующем `node_modules`.
* **Хуки переписаны под стек**: `.githooks/pre-commit` не пропускает секреты, изменения
  `archive/` и изменения `woof-ts/` без `check_all.sh`; установка — `tools/install_hooks.sh`.
  В песочнице `.git` стирается между ходами, поэтому хук не всегда можно поставить — это
  сообщается явно.
* **Регрессия стенда не изменилась** после всех правок политики: seed 42, 150 решений →
  `4323 / 24 / 22 / 2 / шаг 12` (×2, детерминизм).

## Как дописывать

1. Новая запись — с датой и тем, **чем проверено** (команда, тест, evidence-файл).
2. Числа, привязанные к сборке игры или к бюджету прогона, — с этим бюджетом рядом.
3. Отрицательный результат («кандидат отклонён», «приём не дал прироста на val») пишем
   так же, как положительный: это данные о проверенном пространстве.
4. Строки не переписываем задним числом: изменившийся вывод — новая датированная строка
   со ссылкой на прежнюю.

## 2026-09-18 (вечер) — что поймал полный гейт и как исправлен контур обучения

* **`check_all.sh` падал дважды, и оба раза по делу.** (1) Проверка объявления цели искала
  строку в нижнем регистре через `grep -i`, а в локали C байты «Ц»/«ц» не складываются —
  исправлено на литеральное `grep -qF`. (2) Гейт не проверял, что **страж переобучения
  согласован с числами**: добавлена проверка «если `valMean < baselineValMean`, в отчёте
  обязан быть вердикт ПЕРЕОБУЧЕНИЕ, и наоборот», плюс наличие `objective` и `warnings` в
  `best.json` и базового измерения в истории. Чем проверено: прогон шага 7 на
  `/tmp/woof_learn2` — страж согласован (`val=-12.678 < база=-12.418`, вердикт есть).
* **Порог принятия поднят с 0 до 1.0 пункта скора** (`--min-delta`, значение по умолчанию).
  Причина измерена: контур принял кандидат `searchActions: 12 → 10` с приростом **+0.05** —
  это шум, а не улучшение. После правки тот же прогон того же кандидата отклонил. Это
  правило поиска, а не целевая функция: веса не менялись, изменение объявлено до следующего
  замера (здесь и в `woof-ts/LEARNING.md`).
* **Контур объявляет слабые места до измерения**: `warnings` в консоли, отчёте и `best.json`
  — один train-сид, меньше трёх val-сидов, бюджет короче 100 решений. Отчёт получил раздел
  «Вердикт» (переобучение / не хуже базы / val не измерялся) и честную формулировку
  «принято шагов N; отличий в итоговом наборе M» — раньше «принято 3» при двух отличиях
  читалось как расхождение (один параметр принимался дважды: `navMaxSteps 80→70→90`).
* **Подбор с тремя val-сидами подтверждает перенос найденного порога**:
  `--steps 60 --train-seeds 42 --val-seeds 43,44,45 --groups quest` → принято
  `levelDeltaMax: 1→0`; train −71.56 → **116.52**, val −3.76 → **72.95** (11 прогонов, 182 с).
  Для сравнения: тот же набор на одном val-сиде дал −14.02 → −13.95, то есть ничего.
  Вывод прежний и теперь измерен с двух сторон: **один val-сид не отличает улучшение от шума**.
* **Пре-коммит хук создан заново** (`.githooks/pre-commit`): секреты в индексе, archive/
  (удаления и переименования запрещены, правки — только append-only в `archive/**/README.md`),
  мусор (`node_modules`, `dist`, `dist-env`, симлинк `game`, `evidence/tuning/`), изменения
  `woof-ts/` без `check_all.sh` (`WOOF_HOOK_FAST=1` — громко печатаемый режим с `SKIP_TUNE=1`).
  Логика проверена на временном git-репозитории в 7 сценариях: секрет — отказ; удаление в
  archive — отказ; правка архива задним числом (удалено 2 строки) — отказ; append-only строка
  — принято; `node_modules` — отказ; артефакт сборки — отказ; обычный файл — принято.
  На реальной истории `backup` хук не проверялся (в песочнице `.git` стирается) — пункт D5d
  в `woof-ts/ROADMAP.md`.
* **Evidence бриджа переехал в отслеживаемую папку**: `evidence/<дата>/bridge-smoke-seed42.json`
  (было `evidence/tuning/…`, которая в `.gitignore`), и гейт проверяет, что в нём объявлены
  `transport.kind=ndjson`, `entityTemplates=false`, `questStateApi="observed"` — доказательство
  без объявленных возможностей неатрибутируемо.

## 2026-09-18 (поздний вечер) — имя сданного квеста в evidence

* **Дефект найден чтением собственного evidence**: `summary.turnIns[0].questId` был `"?"`.
  Причина: прирост счётчика сдач виден на шаге ПОСЛЕ сдачи, а к этому моменту квест закрыт,
  фаза пуста и `fsm.questId === null`. Доказательство «сдан квест» без имени квеста —
  неполное доказательство.
* **Правка** (`src/core/agent.ts`): поле `lastKnownQuestId` заполняется после каждого
  `fsm.syncFrom`, пока имя известно; сверка берёт `fsm.questId ?? lastKnownQuestId ?? '?'`.
  Результат на том же прогоне: `turnIns: [{step:6, worldStep:157,
  questId:"q_hub_know_your_numbers", total:1}]`, `SUMMARY` не изменился
  (`5530 / 2 / 0 / 1 / шаг 6`) — регрессии нет, полный гейт зелёный.
* Попутно: `tools/build_env.sh` печатал подсказку `--steps 50`, хотя гейт запускает
  `--steps 120`; напечатанное обязано совпадать с реально выполняемым.

## 2026-09-18 (ночь) — дерево игры, порядок резолвера расширений и ложный след `.game-cjs`

* **Симптом с машины владельца**: `npm run build` проходит, `npm test` падает с
  `ReferenceError: exports is not defined in ES module scope`; в дереве игры `D:\woc-game`
  нашлось 523 файла `.js`, а `game/package.json` объявляет `"type": "module"`.
* **Корень, подтверждённый фактами**: в чистом sparse-клоне upstream
  (`levy-street/world-of-claudecraft` v0.43.2) **793 `.ts` и 0 `.js`** — значит 523 `.js`
  на машине владельца это собранный вывод, попавший в дерево игры. Наши импорты игры
  без расширения (`'../game/src/sim/obs'`, `'../../game/src/sim/data'`), а порядок
  резолвера у инструментов разный: **esbuild — `.tsx, .ts, .jsx, .js` (исходник раньше),
  Vite/vitest — `.mjs, .js, .mts, .ts` (сборка раньше)**. Отсюда «сборка зелёная, тесты
  красные»: esbuild брал `obs.ts`, vitest — `obs.js`.
* **Репродукция и проверка фикса** (три сценария; дерево игры не модифицировалось —
  работали на копии): рядом лежат `obs.ts` (`NUM_ACTIONS=2`) и собранный `obs.js` (999):
  (1) дефолтный конфиг vitest → загрузилось **999**, то есть `.js` победил;
  (2) конфиг с `resolve.extensions: ['.mts', '.ts', '.tsx', '.mjs', '.js', '.jsx', '.json']`
  → загрузилось **2**, то есть `.ts`; (3) чистое дерево (только `.ts`) → **2** и с дефолтом.
  Тот же порядок проверен на реальном проекте: при грязном дереве `tests/facts.test.ts`
  с новым конфигом — 9/9 прошли (факты взяты из `.ts`).
* **Что добавлено в репозиторий**: `resolve.extensions` в `woof-ts/vitest.config.ts`
  (факты обязаны браться из исходников upstream, а не из чьей-то сборки);
  секция C в `woof-ts/tools/verify_facts.ts` — громкий отказ, если в `src/sim` или
  `headless` дерева игры появились `.js/.cjs/.mjs`, с текстом что делать;
  `.game-cjs/` в `.gitignore` и в пре-коммит хуке; пункт 4 в `AGENTS.md`
  §«What to Never Do» расширен тем же запретом.
* **Почему алиасы на `.game-cjs` — не лечение**: переименованный `.cjs` остаётся CJS,
  `tsc` теряет типы игровых модулей, `verify_facts` и `tests/facts.test.ts` начинают
  сверяться с рукописной копией вместо upstream (ровно то, что запрещено правилом линии),
  а 523 файла копии пытаются попасть в коммит.

## 2026-09-18 (поздняя ночь) — «upstream обновился, obs=587, farm исчез»: проверка показала обратное

* **Заявление исполнителя на машине владельца**: дерево игры обновилось upstream —
  `obs=587`, тип цели `farm` исчез; вывод «это не только CJS/ESM».
* **Проверка тремя независимыми деревьями** (одно и то же `tools/verify_facts.ts`):

  | Дерево | Версия | .ts / .js | obs | actions | квестов | цели |
  |---|---|---|---|---|---|---|
  | наш клон `~/woc-game` | 0.43.2 | 793 / 0 | 607 | 61 | 224 | kill=106, collect=76, interact=60, gather=4, escort=4, farm=2 |
  | свежий клон `main` = `f4b0261` | 0.43.2 | 793 / 0 | 607 | 61 | 224 | те же |
  | тег `v0.43.1` = `5e903cb` | 0.43.1 | — | 607 | 61 | 224 | те же |

  `git ls-remote` подтверждает: `main` = `f4b0261511ceea49ddd97454710ba0d315f0f76f` =
  тег `v0.43.2`. Все три дерева дают `сверка фактов: OK`.
* **Вывод**: upstream не менялся; `obs=587` и «farm исчез» — артефакт загрязнённого
  дерева или копии `.game-cjs` (то есть ровно того, что исполнитель чинил), а не факт
  об игре. Правило: заявление о дрейфе upstream проверяется `ls-remote` + свежим клоном
  + `verify_facts`, а не показаниями текущего дерева.
* **Что добавлено, чтобы паника не повторилась**: `tools/setup_game.sh` и
  `tools/fix_windows_env.ps1` пинят версию игры (`GAME_REF=v0.43.2`,
  `GAME_EXPECTED_VERSION=0.43.2`, в PS — параметры `-GameRef`/`-ExpectedVersion`) и
  **падают** с инструкцией, если версия дерева другая или в `src/sim`/`headless` есть
  собранный вывод. Переезд на новую версию игры — отдельная задача `ROADMAP.md` A5.


## 2026-09-18 (вечер) — живой мир: первая рантайм-зависимость и два дерева игры

* **`ws` — единственная рантайм-зависимость линии.** Причина: Node 20 не имеет
  глобального `WebSocket`, а upstream делает ровно так же — игра зависит от `ws@^8.21.0`,
  её Node-бот `bot/gateway.ts` импортирует `ws` и инжектирует `socketFactory`
  (`tsconfig.bot.json`: `types:["node"]`, lib ES2022, без DOM). Порядок разрешения в
  `src/bridge/ws_socket.ts`: инжектированная фабрика → глобальный `WebSocket` (Node 22+)
  → `ws` → громкий ПРОВАЛ (`NO_SOCKET_MESSAGE` называет оба лекарства). В бандле `ws`
  внешний (`--external:ws`): стенд и бридж собираются и без него.
* **Два дерева игры, две роли.** `~/woc-game` (sparse, `tools/setup_game.sh`) — дерево
  фактов агента: `src/sim`, `src/world_api*`, `headless`, `python`. Хостинг живого мира
  требует ПОЛНОГО чекаута (`docker-compose.yml` собирает образ из контекста репозитория,
  `build: context: .`; публичного образа upstream не публикует) — `tools/run_world.sh`
  кладёт его в `~/woc-world` и пинит ту же версию 0.43.2: мир и факты разных версий —
  две разные игры. WS-транспорт импортирует `src/net/**`, которого нет в лёгком дереве,
  поэтому для A1.3 симлинк `game` переводится на полный чекаут (`--link-game`).
  Полный чекаут с `docs/` и `tests/` — гигабайты: в песочнице его держать нельзя
  (предел снимка ~128 МБ), клонируем на время и удаляем.
* **Протокол живого мира (v0.43.2), факты:** первый кадр — `buildWebSocketAuthMessage`
  (`src/net/world_auth_message.ts`); сервер сверяет каждое объявленное поле по точному
  равенству и **молча понижает** сессию на legacy-wire при отсутствии поля
  (`server/ws_auth.ts`); команды — `{t:'cmd', cmd: <ClientCommand>, …}`
  (`src/net/online.ts:2222-2235`); движение — отдельный провод `movementWire: 2`
  (`sendMovementFrame`), не команда; отказ всегда приходит кадром `{t:'error'}` до
  закрытия сокета; `DISPATCH_ONLY_COMMANDS` (`src/world_api.ts:855`) — dev-читы
  (`dev_level`, `dev_teleport`, `dev_give`, `dev_complete_quest`, …) и RL-токен
  `targetNearest`: агент их не отправляет, и это закреплено тестом, а не намерением.
* **Один персонаж = один сеанс.** `planJoin` (`server/linkdead.ts`) отвечает
  `reject 'character already in world'`; повторный вход — только
  `POST /api/characters/:id/takeover`. Отсюда схема A: у агента свой персонаж.
* **Возможности мира выводятся, а не объявляются.** `liveWorldCapabilities()` берёт
  `targetSelection`/`abandonQuest`/`vendor`/`partyLootRolls` из таблицы `COMMAND_NAMES`
  upstream (v0.43.2: `target`, `tab`, `abandon`, `buy`, `sell` есть; токены ролов ищет
  `lootRollTokens()`), а всё неизмеренное объявляет консервативно
  (`entityIdentity='slot'`, `questStateApi='none'`, `entityTemplates=false`, …):
  заявленная возможность разрешает политике на неё опираться. `latencyMs` — обязательный
  параметр без дефолта: выдуманная задержка была бы подделкой доказательства.
* **Проверка без docker.** `tools/run_world.sh` прогнан исполнением с заглушкой `docker`
  в PATH: пин версии (провал на 0.41.4 с командами лечения), создание `.env`, генерация
  `POSTGRES_PASSWORD` (64 hex, в журнал не попадает, `DATABASE_URL` согласован),
  `compose up -d --build postgres game`, провал готовности с выводом журнала,
  предупреждение о привилегированном прогоне при `ALLOW_DEV_COMMANDS=1`. С настоящим
  docker не проверялось — его в песочнице нет; это объявлено, а не скрыто.
* **Грабли:** `npm install` в песочнице убирает симлинк `woof-ts/game` — после установки
  зависимостей проверять `readlink game` и пересоздавать (`ln -sfn ~/woc-game game`),
  иначе tsc сыплет «Cannot find module '../game/src/sim/obs'». `git sparse-checkout add`
  в cone-режиме не принимает ведущий слэш и отдельные файлы (только каталоги; файлы корня
  и родителей указанных каталогов попадают сами). `--reporter=basic` в vitest 4 отсутствует:
  vitest пытается загрузить его как кастомный модуль и падает с `ERR_LOAD_URL`.

* **Грабли живого провода (из кода upstream-ботов):** чат и `/dev`-читы — это КОМАНДЫ
  (`{t:'cmd', cmd:'chat', text}`); кадр `{t:'chat'}` верхнего уровня сервер выбрасывает
  молча, и скрипт верит, что боты прокачаны. Снапшоты дельта-кодированы: отсутствующее
  тяжёлое поле `self` (`inv, equip, qlog, qdone, cds, stats, weapon, party, trade, duel`)
  означает «как раньше», а сущности делятся на полные (с полями личности `k, tid, nm, lv,
  sc, c, dgn`) и лёгкие (наследуют их); `snap.keep` — живые, но не изменившиеся id, а чего
  нет ни в `ents`, ни в `keep` — вышло из интереса. Отказ входа при смене эпохи раскладки
  отличается по литералу `ONLINE_WORLD_INCOMPATIBLE_MESSAGE`. Минимальный кадр
  `worldAuthMessage` = `{t, token, character}` понижает сессию на legacy-wire, поэтому
  наш `buildAuthFrame` шлёт полный набор wire-версий.

## 2026-09-19 — археология: что именно было «почти сделано» в браузерной линии (`woof-agent/`)

Проверено в песочнице (java 11, node 20), не по документам: `bash tools/build.sh` →
`build ok -> build/classes (28 files)`; `bash tools/run_tests.sh` → `tests passed=3 failed=0`;
`bash tools/run_e2e.sh` → `УСПЕХ: квест сдан обоими путями (Node-мост и Java-мост),
нарушений контракта нет`, `[Agent] SUMMARY steps=120 kills=3 deaths=0 quests_done=1
first_turn_in_step=8`. Значит J1–J6 замороженной линии до сих пор воспроизводятся.

* **Что было рабочим эталоном (и лежит в репозитории, 1569 строк):**
  `woof-agent/tools/ref/browser_bridge.cjs` (145, HTTP-мост :8791, `info` — ВСЕГДА плоский
  снимок, dispatch `snapshot|step|navigate|raw_move|respawn|explore|health`, очередь команд
  продолжается только после успешного `client.health()`), `tools/ref/snapshot.cjs` (456) и
  `tools/ref/actions.cjs` (968, 13 навыков).
* **Поверхность мира в браузере (из `snapshot.cjs`/`actions.cjs`) — богаче RL-обсервации:**
  `window.__game.sim.player` (`hp,maxHp,level,facing,dead`), абсолютные `player_pos=[x,z]`,
  `sim.entities.values()`, `sim.entitiesNear(pos,r)`, `sim.bags`/`bagCapacity`,
  `sim.recipeList`, `sim.stationPlacements`, `sim.known[]` (ResolvedAbility),
  и — главное — `sim.questState(qid)` помечен в эталоне как **AUTHORITATIVE in offline
  (проверено 2026-08-27)**, то есть состояние квеста полное, а не «0 = нет или уже сдан»,
  как в `src/sim/obs.ts`. Действия: `sim.targetEntity(id)`, `sim.startAutoAttack()`,
  `sim.interact()`, `sim.lootCorpse(mobId,pid)`, `sim.acceptQuest(qid,null,pid)`,
  `sim.turnInQuest(qid)`, `sim.sellItem(id,n)`, `sim.addEntity({...})`.
* **Грабли, найденные той линией экспериментально (не повторять):**
  (1) `controller.face()` — **no-op**, поворот делается только через
  `g.controller.move({[kind]:true}, desiredFacing)`; (2) единый порог курса в chase+face
  даёт автоколебание (курс проскакивает нуль, знак flipping) — нужен гистерезис;
  (3) в браузере висят **мёртвые вкладки с тем же URL**, поэтому `CdpClient.acquirePage()`
  обязан выбирать вкладку пробой `window.__game.sim` + `primaryId` + существующей сущностью
  игрока, иначе бот «работает» ни с чем; (4) `farm` держит вкладку ~17 с → нужна ограниченная
  очередь команд и watchdog; (5) страница **не надёжно** отдаёт `sim.questDefs`/`sim.npcDefs`,
  из-за чего эталон их захардкодил — в активной линии так нельзя: контент берём из
  `woof-ts/game/content/**`, а не из страницы.
* **Чего в репозитории НЕТ (это и есть «почти»):** J7 — `CdpBackend` не портирован (честно
  отвечает HTTP 500 + «BRIDGE-PORT», проверено `TestJavaBridge`); отсутствуют
  `src/bridge/game_client.cjs` (151, CDP-транспорт), `cmd_queue.cjs` (77), `heading.cjs`/
  `fence_hop.cjs`/`quests_done.cjs` (145) — ~230 строк транспорта и обвязки. J8 — **живой
  прогон в реальной игре не выполнялся никогда**: логов живого прогона в репозитории нет,
  есть только SUMMARY с полигона. J9 (`q_spiders` целиком), J10 (устойчивость), J11
  (`run_tests.sh` поднимает `fake_cdp` дважды → `EADDRINUSE` в каждом прогоне; готовность
  фейка проверяется «порт занят», а не `kill -0 $PID` + строка `listening` — воспроизвёл),
  J12 (`BridgeServer`/`WoofMcpServer` на `0.0.0.0` без аутентификации, а `woof_build` через
  MCP запускает `javac`) — не закрыты.
* **Что уже перенесено в активную линию:** транспорт CDP написан заново в
  `src/bridge/cdp_client.ts` (выбор вкладки, id-сопоставление `Runtime.evaluate`, запрет
  `Page.reload`/`Page.navigate` и перехвата ввода) — это ровно тот отсутствующий
  `game_client.cjs`. Наследовать из `woof-agent/` надо ПОВЕДЕНИЕ (`snapshot.cjs`,
  `actions.cjs`, `SkillIndex`) и приёмку, а не Java-сборку: AGENTS.md отводит замороженной
  линии роль «источник архитектуры».

## 2026-09-19 (вторая половина) — браузерная линия C1 построена, полигон J8 зелёный

Что добавлено в активную линию (проверяется `bash tools/check_all.sh`, шаг 8):
`src/world/browser_world.ts` (мир в браузере: снимок страницы → `WorldModel`, действия через
публичные `sim.*`, навигация с гистерезисом), `src/core/browser_agent.ts` (асинхронный цикл,
переиспользует `GoalFSM`/`ArbitrationLayer`/`WorldMemory`/`SkillLibrary` без изменений),
`acquireLivePage`/`livenessExpression` в `src/bridge/cdp_client.ts`, `tools/cdp_probe.ts`,
`tools/run_offline.ts`, полигон `tests/browser_world.test.ts` (26 проверок), runbook
`woof-ts/OFFLINE-BROWSER.md`. В `world.ts` транспорт дополнен значением `cdp`, словарь команд —
`page-api`; заодно исправлена печать возможностей: было
`состояние любого квеста=${c.questStateApi ? 'да' : 'нет'}` — строка всегда истинна, поэтому
при `questStateApi:'observed'` и даже `'none'` печаталось «да». Теперь печатается само значение.

* **Грабли полигона (пойманы 2026-09-19, стоят в гейте):**
  (1) `set -o pipefail` + `grep -q` в конвейере с vitest = ЛОЖНЫЙ ПРОВАЛ: `grep -q` выходит
  сразу после совпадения, vitest получает SIGPIPE (141), и конвейер «провален» при зелёных
  тестах. Лечится ловлей вывода в переменную и сверкой через `[[ "$out" =~ … ]]`, без конвейера.
  (2) `NO_COLOR=1` обязателен: в выводе vitest ANSI-коды стоят МЕЖДУ `Tests` и `26 passed`,
  поэтому обычный grep по строке итога не совпадает.
  (3) Фейковая страница обязана держать сущности и игрока в `pos:{x,y,z}`, а не в `x`/`z`:
  страничный код читает `e.pos.x`, и расхождение молча превращает мир в пустой.
  (4) В ветке побега нужен guard `if (!e || !e.pos) continue;` — страница отдаёт сущности без
  `pos` (в снимке эталона такой guard был, в моей первой версии побега — нет: падало на чтении).
  (5) Цель приобретается ПОСЛЕ наблюдения, а страница сбрасывает `targetId` в кадр смерти,
  поэтому наблюдаемый счётчик убийств обязан помнить цель с прошлого кадра (`prevTargetId`
  выставляется и в `act('farm')`), иначе `kills` всегда 0 при реально убитых мобах.
  (6) Фейк обязан отдавать журнал квеста (`sim.questLog`/`world.questLog`) той же структурой,
  которую заводит `acceptQuest`, иначе снимок приходит без `resolvedCounts` и прогресса.
* **Что осталось до J8:** только машина владельца (игра :5173 + Chrome :9222). Порядок,
  пороги и границы среды — `woof-ts/OFFLINE-BROWSER.md`.
