# Как думать в этом проекте

Короткий список правил, которые уже спасали работу. Это не «ценности», а следствия
из конкретных провалов — каждый пункт когда-то стоил нам времени или доверия.

## 1. Поведение реальной игры важнее тестов

Тесты доказывают, что код согласован сам с собой. Игра доказывает, что он нужен.
Если тест зелёный, а в игре квестов ноль — врёт тест или стенд, а не игра.
Живая проверка идёт последней и она главная.

## 2. Нет молчаливых откатов

Любая подмена (недоступное устройство → CPU, чужая таблица квестов → «подставим число»,
неизвестный навык → no-op, мёртвая вкладка → «первая попавшаяся») должна **кричать**.
Именно тихий фолбэк дал нам «0 квестов» при зелёных тестах. Правило: лучше упасть
с причиной, чем «сработать» не тем.

## 3. Контроль или не считается

Любое «наш вариант справляется» проверяется против контролей **на тех же сидах и тех же
входах**: выключенная компонента, необученная версия, случайная замена той же плотности,
простой baseline. Для Java-бота это значит: не «агент сдал квест», а «агент сдал квест,
а заглушка, случайный выбор и отключённый гейт фаз — не сдали». Если контроли показывают
то же самое, честный вывод — «эффекта нет», и он публикуется.

Эталонный пример такого замера — приёмка fly-линии (`our` / `rewired` / `er` / `silenced` /
`untrained` / `MLP` на трёх сидах). Линия архивирована 2026-09-18, числа и метод —
в `archive/fly-line/README.md`.

## 4. Демо ≠ результат

Агент, который ходит и машет руками, не доказывает ничего. Доказывают метрики с порогами,
объявленными заранее, и контроли. Пороги объявляются **до** замера, чтобы не подгонять их
под результат. Для Java-линии порог — не «красиво двигается», а `quests_done ≥ 1` на живой
игре и цепочка `q_spiders` целиком (J8/J9 в `woof-agent/ROADMAP.md`).

## 5. Меряем, а не обещаем

«Должно быть быстрее» — не факт. Факт — замер с синхронизацией и указанием конфигурации:
устройство, батч, версия рантайма. Числа пишем вместе с конфигурацией, иначе их нельзя ни
сравнить, ни повторить. Для Java-линии это шаги агента, киллы, смерти, номер шага первой
сдачи квеста (`[Agent] SUMMARY`), а не «бот вроде играет».

## 6. Истина — на remote

Локальные коммиты, «вроде бы запушено» и память о том, что было — не доказательства.
Перед любым утверждением о ветке: `git fetch` / свежий клон и посмотреть, что реально
лежит. Мы уже ловили расхождение: три коммита локально имели ту же суть, но другие SHA,
чем на remote.

## 7. Пуш — не приёмка

Код в ветке ничего не доказывает. Доказывают: воспроизводимая команда проверки,
её вывод и — для живой линии — прогон на реальной игре.

## 8. Сначала корневая причина, потом правка

Симптом («квесты = 0») правится после того, как найдена причина (молчаливый фолбэк
в таблице оракула). Иначе лечим температуру.

## 9. Не тащить чужое вслепую

Код без лицензии — только как идея. Заимствование — с указанием источника, лицензии
и того, что именно взято. Чужие данные с лицензией CC BY (в архиве — MaleCNS, CC BY 4.0)
требуют атрибуции даже в производных артефактах и фигурах.

## 10. Главное не терять

Задача живёт дольше одной сессии: цель, инварианты и текущий статус должны лежать
в файлах (`knowledge/`, `ROADMAP.md`, навык hermes), а не в голове агента.
Правило простое: если это важно и его нельзя восстановить из репозитория — оно
обязано быть записано сегодня.

## 11. Датированные дополнения (2026-09-18, линия `woof-ts`)

Эти правила выведены из переноса агента на лёгкий стек и из первого прогона через бридж.
Они не отменяют §§1–10, а уточняют их для мира, который живёт не в нашем процессе.

* **Мир — за интерфейсом, а не внутри.** Политика обязана работать и тогда, когда мир
  в другом процессе и отдаёт только вектор наблюдения. Если правило политики читает
  данные, которых по проводу нет, это баг политики, а не ограничение мира.
* **Возможности объявляются до прогона и печатаются.** `capabilities` (состояние квестов,
  идентичность и вид сущностей, счётчики урона, API предметов) — часть результата:
  прогон без них неатрибутируем, а привилегия стенда, выданная за переносимый результат,
  — самообман (§4).
* **Отсутствие данных не подделывается.** Нет id сущности — отрицательный синтетический id
  и `entityIdentity='slot'`; нет накопленного урона — `damageCounters=false` и другой
  наблюдаемый признак успеха (падение доли HP цели). Нули вместо «не знаю» — это §2.
* **Пороги — таблица с диапазонами, значения — из неё.** Литерал в политике не подбирается,
  не печатается и не атрибутируется; выход за объявленный диапазон — исключение.
* **Улучшение себя — по объявленной цели и с историей.** Целевая функция и сплит сидов
  объявлены до измерения, каждый кандидат — отдельный процесс, каждый отказ записан.
  «Подобрал, стало лучше» без истории и без val-сидов — это §4 (демо).
* **Единицы не меняются между прогонами.** `world_steps` = шаги мира (действия × frameSkip)
  в обоих транспортах; сравнение «4323 против 5530» осмысленно только при объявленном
  бюджете решений и одинаковых единицах.
* **Факты игры — импортом.** Ни одного числа, переписанного руками: раскладка obs, размеры
  мира, уровни мобов, квесты берутся из дерева игры и сверяются `tools/verify_facts.ts`.
  Хардкод `obs=607` в архивной линии (баг B3) — ровно тот урок, который мы не повторяем.
