[pytest]
# Маркеры, которые реально используются в наборе. Без регистрации pytest
# печатает PytestUnknownMarkWarning на каждый прогон — шум, в котором легко
# пропустить настоящее предупреждение.
markers =
    integration: требует живой мост/игру (не запускается в обычном прогоне)
