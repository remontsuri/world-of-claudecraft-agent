# Роадмап woof-agent

Статусы: ✅ сделано (есть проверка) · 🔄 в работе · ⬜ не начато.
Правило проекта: пункт закрывается **только** с проверкой, которую можно повторить
одной командой. «Работает у меня» — не закрытие.

## Java-бот (активная линия)

| # | Что | Критерий закрытия | Статус |
|---|---|---|---|
| J1 | Аудит состояния агента перед портом | `AUDIT-2026-09-16.md` (что сломано, что заглушка) | ✅ |
| J2 | Контракт моста на Java (`BridgeServer`) | `TestJavaBridge`: контракт, `idx`, неизвестное действие | ✅ |
| J3 | Бэкенд «Java впереди, Node за ним» (`UpstreamBackend`) | e2e-фаза 1: квест сдан через Java-мост | ✅ |
| J4 | Выбор **живой** вкладки CDP (`CdpClient.acquirePage`) | `TestCdpClient`: мёртвая вкладка идёт первой, выбирается живая | ✅ |
| J5 | Три набора тестов без игры | `tools/run_tests.sh` → `passed=3 failed=0` | ✅ |
| J6 | Сквозной e2e на фейках | `tools/run_e2e.sh` → `УСПЕХ ... нарушений контракта нет` | ✅ |
| J7 | Полный порт `CdpBackend` (без Node) | e2e-фаза 2 идёт **без** `--upstream`, `TestJavaBridge` больше не ждёт 500 | ⬜ |
| J8 | Живой прогон в реальной игре | `[Agent] SUMMARY` с `quests_done ≥ 1` на живой игре, `kills ≥ 1` | ⬜ |
| J9 | Квестовая цепочка `q_spiders` целиком | 6 пауков → 4 шёлка → сдача, без ручных подсказок | ⬜ |
| J10 | Устойчивость: игра/мост упали | агент переживает падение моста, пишет причину, не «зависает молча» | ⬜ |
| J11 | Один владелец фейкового CDP в тестах | в логе `run_tests.sh` нет `EADDRINUSE`; готовность фейка проверяется не «порт занят», а `kill -0 $PID` + строка `listening` | ⬜ |
| J12 | Мост и MCP слушают только loopback | `BridgeServer`/`WoofMcpServer` по умолчанию на `127.0.0.1`; `0.0.0.0` — только явным флагом; доступ по токену | ⬜ |

**J7 — что именно осталось** (детали в `BRIDGE-PORT.md`): перенести из
`tools/ref/browser_bridge.cjs` логику `snapshot` (сбор мира из `window.__game.sim`),
`navigate`/`raw_move` (ввод в контроллер), `respawn` и `explore`. До этого момента
`CdpBackend` обязан отвечать `HTTP 500` + `BRIDGE-PORT` в теле — это уже проверено.

**J8 — что нужно от машины**: игра на `:5173`, CDP на `:9222`, java 11, GPU не нужен.
Порядок: `build.sh` → `run_tests.sh` → `BridgeMain` → `VoyagerAgent --steps 200`.

## Hermes (агент-исполнитель)

| # | Что | Критерий | Статус |
|---|---|---|---|
| H1 | Инструкции проекта для hermes | `hermes/README.md` + ссылки из корневого `AGENTS.md` | ✅ |
| H2 | Инструменты, которые hermes обязан запускать | `hermes/TOOLS.md`: что, когда, что считается успехом | ✅ |
| H3 | Навык «главная цель» | `hermes/skills/woc-master-goal/SKILL.md` | ✅ |
| H4 | Хуки (не забывать главное, не портить репо) | `bash hermes/hooks/selftest.sh` → `passed=17 failed=0` | ✅ (осталась регистрация в конфиге hermes) |
| H4b | Регистрация хуков в hermes | `hooks.json` сверен с рабочим конфигом, сессия начинается с напоминания | ⬜ |
| H5 | MCP-сервер `WoofMcpServer` на `:8792` | hermes видит инструменты агента, живой вызов проходит | ⬜ |
| H6 | Инструменты MCP без заглушек | `woof_logs`, `woof_config`, `woof_test`, `woof_execute_action` отвечают делом, а не «not yet implemented»; `woof_build` без хардкода путей Windows | ⬜ |

## Fly-линия — в архиве

Линия коннектома (схема на 8835 нейронов, PPO-читаут, контроли `rewired`/`er`, пороги
M0–M4) **архивирована 2026-09-18** целиком, 180 файлов, без удалений: `archive/fly-line/`.
Пункты F1–F6, объявленные пороги, фактические числа F8 и незакрытые дефекты линии
(B1–B8, B13) — в `archive/fly-line/README.md`. Отсюда они убраны, чтобы роадмап
показывал только живую работу: разморозка линии — отдельная задача владельца.

## Политика веток

`master` не трогаем, работа идёт в `backup` (fast-forward, без `--force`) —
подробно в `GIT-WORKFLOW.md`. Проверка перед пушем там же, в протоколе из шести шагов.

## Долги и мелочи, которые уже поймали

* `run_tests.sh`: осиротевший `fake_cdp.cjs` держал порты — теперь есть проверка портов. ✅
* `run_tests.sh` поднимает `fake_cdp` **дважды** (скрипт и `TestCdpClient`) на 9231/9232 —
  в каждом прогоне `EADDRINUSE`, тест идёт против того экземпляра, который занял порт
  первым. Найдено 2026-09-18, пункт J11. ❌
* `BridgeServer` и `WoofMcpServer` bind'ятся на `0.0.0.0` без аутентификации, а `woof_build`
  через MCP запускает `javac`. Найдено 2026-09-18, пункт J12. ❌
* `TestJavaBridge`: не читал тело отказа (`errorStream`) — падал вместо проверки. ✅
* Русский текст java/javac превращался в `?????` — добавлен `file.encoding=UTF-8`. ✅
* `run_e2e.sh`: те же флаги UTF-8 (2 вызова java). ✅
