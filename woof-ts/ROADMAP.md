# ROADMAP линии woof-ts

Долг и план активной линии. Пункты закрываются только живым прогоном и записью в
`PROGRESS-<дата>.md` + `../knowledge/woof-ts.md`; «кажется, работает» не закрывает ничего.
Статусы: ✅ закрыто и доказано · 🟡 сделано частично · ⬜ открыто.

## Закрыто (2026-09-18)

* ✅ **D4. Тесты.** vitest: 6 файлов, 62 теста, включая 15 на живом env-сервере игры
  (`tests/bridge_env.test.ts`). Есть тесты, которые умеют падать: другой сид даёт другой
  эпизод; `countItem` без возможности бросает; действие вне словаря — отказ мира;
  obs другой длины — исключение.
* ✅ **D5a. Обвязка репозитория.** `tools/setup_game.sh` (клон игры + зависимости + сборка;
  `node_modules` в песочнице стирается каждый ход), `tools/build_env.sh`,
  `tools/check_all.sh` (8 шагов, тот же гейт зовёт пре-коммит хук), `tools/verify_facts.ts`.
* ✅ **D5b. Правила и документы под новый стек.** Переписаны: `../AGENTS.md`,
  `../README.md`, `.githooks/pre-commit`, `../knowledge/README.md`, `../knowledge/pitfalls.md`
  (датированные разделы), `../knowledge/principles.md` (§11), `../knowledge/verification.md`
  (§4b), созданы `../knowledge/woof-ts.md`, `BRIDGE.md`, `LEARNING.md`, `README.md` линии.
* ✅ **Бридж к миру.** Транспорт `ndjson-stdio` к headless env-серверу игры: контракт
  upstream, раскладка obs из фактов, синхронный канал (воркер + `SharedArrayBuffer` +
  `Atomics`), эквивалентность наблюдения со стендом доказана, прогон состоялся
  (seed 42, 120 решений: `kills=2 deaths=0 quests_done=1 first_turn_in_step=6`).
* ✅ **Контур самоулучшения.** `src/learn/`: цель объявлена до измерения, подбор отдельными
  процессами по объявленной сетке, train/val сплит без пересечения, append-only история,
  `best.json` + `last_report.md`. Demo-подбор прошёл (принят `levelDeltaMax: 1→0` на
  бюджете 60 решений; вывод о зависимости скора от бюджета — в `LEARNING.md`).
* ✅ **Пороги в одной таблице.** `src/policy/params.ts`: 34 параметра с диапазонами и
  шагом; загрузка только через `WOOF_PARAMS`; выход за диапазон — исключение; дифф
  печатается до прогона и пишется в evidence.

## Открыто

* ⬜ **D5c. Коммит и пуш.** В песочнице `.git` стирается между ходами, поэтому изменения
  не закоммичены. На своей машине: `git clone`, `bash tools/install_hooks.sh`,
  `bash woof-ts/tools/check_all.sh`, коммит в `backup`, пуш **с новым PAT**
  (прежний засвечен в чате — отозвать, не переиспользовать). Протокол — `../GIT-WORKFLOW.md`.
* ⬜ **D5d. Хук проверить на живой истории.** Логика пре-коммита проверена на временном
  git-репозитории (7 сценариев: секрет, удаление в archive, правка архива задним числом,
  append-only строка, node_modules, артефакты сборки, обычный файл), но не на реальной
  истории `backup` — после клона прогнать `git commit --allow-empty -m test` и один
  коммит с правкой `woof-ts/` (гейт должен запуститься).
* ⬜ **D5e. Прогнать `tools/fix_windows_env.ps1` на живой Windows 11.** Сценарий написан
  после того, как на машине владельца дерево игры оказалось загрязнено собранным выводом
  (523 `.js`), а лечением была выбрана копия `.game-cjs/` с алиасами. Статически проверен
  (баланс конструкций, BOM для PowerShell 5.1), но на живой Windows не запускался.
  Критерий: `ОКРУЖЕНИЕ ГОТОВО`, `сверка фактов: OK`, `Test Files 6 passed (6)`,
  `Tests 62 passed (62)`; результат записать в `PROGRESS-<дата>.md`.
* ⬜ **B1. Полный квестовый цикл за бриджем.** Сейчас за бриджем сдан один квест
  (`q_hub_know_your_numbers`). Нужно: цепочка `q_hub → q_wolves → q_greyjaw` и цели
  `collect`/`interact`, где объект виден только в 5 ярдах (придётся искать, а не знать).
  Критерий: `quests_done ≥ 2` при `--transport ndjson` на 300+ решениях.
* ⬜ **B2. Смерти на стенде.** 22 смерти за 150 решений съедают бюджет: лечение — подбор
  `survival`/`combat` на том же бюджете (`--steps 150`, ≥3 train-сида, ≥3 val-сида),
  а не ручная правка порогов. Цель: `deaths ≤ 10` при `quests_done ≥ 2` и `kills ≥ 20`.
* ⬜ **D6. Робастность по сидам.** seed 7: `kills=1, deaths=183` на том же коде — агент
  идёт сквозь лагеря, его накрывает стая, пешком уйти нельзя (мобы быстрее), а у города
  арбитраж выбирает `farm` раньше `navigate`. Лечение: маршрут в обход лагерей
  (центры/радиусы из `CAMPS`, безопасные точки из `hub`/`pois`), боевая политика
  способностей (AoE/защита/самохил вместо «первой готовой»), приоритет похода к пину
  квестодателя над случайным боем. Критерий: не хуже базового на ≥3 сидах из 4 (7, 42, 43, 44).
  В живом мире сид не выбирают, поэтому это часть приёмки.
* ⬜ **D3. `q_greyjaw` не сдан.** Урон по Old Greyjaw есть, убийства нет: не хватает DPS
  и HP. Повторить после B2/D6 на 800+ шагов.
  * **A1. Живой мир: агент — отдельный WS-клиент авторитетного сервера `:8787`.**
    Схема выбрана владельцем 2026-09-18: человек держит мир в браузере, агент играет
    **своим** персонажем и виден в мире как другой игрок. Иначе нельзя: один персонаж =
    один сеанс (`server/linkdead.ts` → reject `'character already in world'`, повторный
    вход только takeover'ом, который выбросит сеанс человека). CDP/9222 не используется —
    это архитектура замороженной Java-линии. Факты и план: `ONLINE-WORLD.md`.
    * ✅ **A1.0 Провод и сокет** (`src/bridge/ws_protocol.ts`, `src/bridge/ws_socket.ts`,
      тесты `tests/ws_live.test.ts`, 21 проверка): кадр рукопожатия из констант upstream
      (`ONLINE_WORLD_AUTH_TYPE = auth-world-29`, wire-версии), команды `{t:'cmd', cmd, …}`,
      запрет dispatch-only токенов (dev-читы и RL-токен `targetNearest`), разбор кадра
      сервера с громким провалом, порядок разрешения сокета; возможности мира **выводятся**
      из таблицы `COMMAND_NAMES` и объявляются консервативно, пока не измерены.
    * ✅ **A1.1 Подъём мира** (`tools/run_world.sh`): полный чекаут + пин версии 0.43.2,
      `.env` и генерация `POSTGRES_PASSWORD` без вывода в журнал, `docker compose up -d
      --build postgres game`, готовность по `/api/status`, предупреждение о привилегированном
      прогоне при `ALLOW_DEV_COMMANDS=1`, флаги `--clone/--no-build/--link-game/--print-only/
      --logs/--down`. **Проверено исполнением с заглушкой docker** (все ветки, включая провал
      версии и провал готовности); с настоящим docker не проверялось — в песочнице его нет.
    * ⬜ **A1.2 Аккаунт и персонаж агента через REST**: `/api/register` → `{token}`,
      `/api/login` → `{token}`, `/api/status`, CRUD персонажей, `Authorization: Bearer`.
      **Готовый образец upstream**: `scripts/mp_integration.mjs` (register, login,
      character CRUD, два клиента в одном мире, движение, бой, чат, персистентность) —
      контракт снят в `UPSTREAM-BOTS.md` §3. Секреты — только из переменных среды,
      в evidence никогда.
    * ⬜ **A1.3 `src/bridge/ws_client.ts` + `src/world/live_world.ts`**: REST-вход,
      WS-сеанс, кадры `hello`/`snap`/`events`/`error`, дельта-слияние `self`
      (`inv, equip, qlog, qdone, cds, stats, weapon, party, trade, duel`) и сущностей
      (`ents` + `keep`, поля личности `k, tid, nm, lv, sc, c, dgn`), движение кадром
      `{t:'input', mi, facing}`, транспорт `--transport ws`. Образцы: `scripts/crypt_raid.mjs`
      (пять играющих ботов, `mergeSelf`/`mergeEnts`) и `scripts/lib/world_auth.mjs`.
      Альтернатива — импорт `ClientWorld` из `src/net/**` (нужен полный чекаут,
      `--link-game`); выбор фиксируется по факту: что поднимается в Node без DOM.
    * ⬜ **A1.4 Движение и темп**: движение — отдельный провод (`movementWire: 2`,
      `sendMovementFrame`), не команда `cmd`; темп в пределах рейт-окон (`msg_rate_limit`,
      `msg_lanes`, `ws_backpressure`, keepalive 10 мин), reconnect/backoff (чистые модули
      игры `src/net/backoff.ts`, `reconnect_policy.ts`). Критерий: 1000 команд без кика;
      обрыв соединения → переподключение без потери эпизода.
    * ⬜ **A1.5 Приёмка**: тот же агент **без правок политики** делает ≥1 убийство и ≥1 квест
      за живым сервером, пока владелец смотрит в браузере; возможности объявлены до прогона;
      измерено то, что объявлено (стабильные id, состояние квестов, templateId, HP, задержка).
    * ⬜ **A1.6 Измерение возможностей**: всё, что в `MeasuredLiveFacts` не измерено,
      объявляется консервативно (`entityIdentity='slot'`, `questStateApi='none'`, …) —
      заявленная возможность разрешает политике на неё опираться, поэтому заявляем только факт.
      План измерения составлен по коду upstream-ботов (`UPSTREAM-BOTS.md` §4): ожидаем
      `entityIdentity='stable'`, `entityTemplates=true` (`tid`), `entityAbsoluteHp=true`
      (`hp`/`mhp`), `absoluteCoords=true` (`self.x/z`), `itemCountApi=true` (`inv`),
      состояние квестов — по серверному `qlog`/`qdone`. Ожидание не объявление:
      объявим только то, что увидим в кадре.
  * ⬜ **A1.7 XP и лидерборд как метрика живого мира.** Арена авторов игры меряет агентов
      живым XP («даётся за квесты, убийства, подземелья и эффективные маршруты, поэтому
      вознаграждается долгосрочное планирование, а не долбёжка кнопок»). У нас целевая
      функция собрана из счётчиков стенда; для живого мира добавить чтение XP/лидерборда
      и объявить новую метрику ДО замера. Основание: `UPSTREAM-BOTS.md` §8.
  * ⬜ **A7. Самоулучшение в живом мире.** Сейчас контур обучения (`src/learn/tune.ts`)
      подбирает пороги офлайн по train/val сидам и со стражем переобучения. Арена
      переписывает **code policies** в production: работает — оставляем, не работает —
      переписываем, без fine-tuning и без человека. Наш шаг: научиться менять навыки
      (`src/skills/`) по итогам живых прогонов, сохранив правила линии — эталон объявлен
      до замера, изменения датированы, откаты видимы в evidence.
  * ⬜ **A8. Два агента на одном мире.** Живой сервер допускает много клиентов
      (`MAX_PLAYERS_PER_REALM`, по умолчанию 5000), а upstream-скрипты уже гоняют
      пять ботов в одной группе (`scripts/crypt_raid.mjs`). Шаг: два наших агента
      (разные параметры) в одном мире, сравнение по объявленной метрике.
  * 🔄 **C1. Агент в браузерном офлайн-мире (CDP).** Отдельная задача, не часть A1:
      `pnpm run dev` → http://localhost:5173 → «Play Offline». Сервера и сокета там нет,
      мир живёт в странице; страница отдаёт `window.__game.sim` (src/main.ts:5048), а канон
      наблюдения/действий `src/sim/obs.ts` (`encodeObs`, `applyAction`) не зависит от Node
      и DOM и в dev-сервере импортируется по URL (`base:'/'`). Движение — через
      `window.__game.controller.move({forward:true}, facing)` (приём upstream для
      браузерных агентов). Ограничения, объявленные до измерения: офлайн-dev всегда с
      `devCommands: import.meta.env.DEV` (привилегированная среда), темп задаёт
      `requestAnimationFrame` (realtime, frameSkip=0, детерминизма нет), страницу
      перезагружать нельзя. Слой уже начат: `src/bridge/cdp_client.ts` (запрет
      Page.reload/navigate и перехвата ввода, выбор вкладки без угадывания, зонд только
      на чтение). Дальше: `tools/cdp_probe.ts` и мир поверх CDP.

    **Наследство замороженной Java-линии (проверено 2026-09-19, приёмка воспроизводится):**
    поведение для C1 уже написано и лежит в `woof-agent/tools/ref/` — `browser_bridge.cjs`
    (145), `snapshot.cjs` (456), `actions.cjs` (968, 13 навыков). Наследуем ПОВЕДЕНИЕ и
    приёмку, не Java-сборку (AGENTS.md: замороженная линия — источник архитектуры).
    Поверхность мира в браузере богаче RL-обсервации: абсолютные `player_pos`,
    `sim.entitiesNear`, `sim.recipeList`, `sim.stationPlacements`, `sim.known[]` и
    `sim.questState(qid)` — в эталоне помечен AUTHORITATIVE in offline (2026-08-27), то есть
    состояние квеста полное. Подробный разбор и грабли — `knowledge/woof-ts.md` от 2026-09-19.
    Подзадачи C1: **C1.1** `tools/cdp_probe.ts` (выбор ЖИВОЙ вкладки пробой `window.__game.sim`
    + `primaryId` + сущность игрока; мёртвые вкладки с тем же URL — задокументированный
    грабель) → критерий: проба на реальной странице отдаёт `hp/pos/level`, на мёртвой —
    честный отказ. **C1.2** `src/world/browser_world.ts`: шов `World` поверх `cdp_client.ts`
    + перенос снимка из `snapshot.cjs`; декларация возможностей — `questStateApi:'full'`,
    координаты абсолютные, темп realtime без детерминизма, среда привилегированная
    (`devCommands`). Контент (`questDefs`/`npcDefs`) — из `woof-ts/game/content/**`, НЕ из
    страницы (эталон их хардкодил: страница отдаёт их ненадёжно). **C1.3** перенос навыков из
    `actions.cjs` по канону индексов `SkillIndex` (13), с гистерезисом курса в chase+face
    (единый порог даёт автоколебание) и ограниченной очередью команд + watchdog (`farm` держит
    вкладку ~17 с). **C1.4** полигон без браузера: переписать `fake_cdp.cjs`/`fake_bridge.cjs`
    под vitest и поднимать фейк ОДИН раз, проверяя готовность строкой `listening` +
    `kill -0 $PID`, а не «порт занят» (долг J11 замороженной линии). **C1.5** мост слушает
    только loopback + токен (долг J12). **C1.6 = J8** живой прогон: игра `pnpm run dev` :5173
    + Chrome `--remote-debugging-port=9222`, критерий — `quests_done ≥ 1` и `kills ≥ 1` без
    правок политики; требует машины владельца (в песочнице нет браузера и полного чекаута
    игры). Живой прогон в этой линии не выполнялся никогда.

    **Статусы C1 (2026-09-19, всё проверяется одной командой `bash tools/check_all.sh`, шаг 8):**
    C1.1 ✅ `tools/cdp_probe.ts` — зонд живой вкладки: карта api страницы (`typeof` каждого
    кандидата), состояния квестов, живые позиции NPC, способности, виды сущностей вне модели;
    мёртвая вкладка отклоняется с причиной, две живые — требование `--target-id`.
    C1.2 ✅ `src/world/browser_world.ts` — снимок страницы → `WorldModel` (чистая функция
    `mapSnapshot`, тестируется без CDP), `capabilities` с `transport:'cdp'` и
    `commandVocabulary:'page-api'`, происхождение счётчиков объявлено (`COUNTERS_PROVENANCE`).
    Контент — из `src/facts.ts` (дерево игры), хардкода квестов нет.
    C1.3 🔄 перенесены `farm`, `loot`, `accept_quest`, `turn_in_quest`, `heal`, `cast_*`,
    `flee`, `respawn`, `noop`, `navigate` (гистерезис 0.35/0.10/1.20, stuck-детектор, побег
    при hp<50%). НЕ перенесены `gather`, `craft`, `craft_item`, `sell_junk`, `equip`, `buy` —
    возвращают `UNSUPPORTED:…`, арбитраж их выключает, и квесты с такими целями не берутся.
    C1.4 ✅ полигон `tests/browser_world.test.ts` (26 проверок, без портов и серверов:
    страничный код исполняется через `new Function` против фейкового `window.__game`),
    включая **приёмку J8 на полигоне**: «взять квест → убить → сдать» даёт `kills>=1`,
    `quests_done>=1`, одно событие сдачи с именем квеста.
    C1.5 ✅ закрыто конструкцией: HTTP-моста и MCP нет, агент говорит с вкладкой напрямую,
    слушать нечему (долг J12 — мост на `0.0.0.0` без токена — снят отсутствием сокета).
    C1.6 ⬜ **живой прогон (J8) — ждёт машины владельца**: игра :5173 + Chrome :9222.
    Порядок и пороги — в `woof-ts/OFFLINE-BROWSER.md`; команды `node dist/cdp_probe.mjs`,
    затем `node dist/run_offline.mjs --steps 200 --json evidence/offline-browser/run.json`.
    Без Chrome оба инструмента отказывают кодом 1 и дают инструкцию (проверено в гейте).
* ⬜ **A2. Профессиональные квесты.** `q_prof_*` в headless-мире непроходимы (нет сбора и
  крафта): сейчас недостижимый квест вычёркивается после провалов. Для живого мира нужен
  честный навык `gather`/`craft`, иначе агент будет брать то, что не может сдать.
* ⬜ **A3. Подбор под бриджем, а не под стендом.** Пороги, найденные на стенде, обязаны
  переноситься на транспорт без привилегий. Прогнать `tune.mjs --transport ndjson` на
  3 train-сидах и сравнить с `learning/best.json`: расхождение — находка для `BRIDGE.md`.
* ⬜ **A5. Переезд на новую версию игры.** Дерево игры — часть воспроизводимости, поэтому
  `tools/setup_game.sh` и `tools/fix_windows_env.ps1` пинят `GAME_REF=v0.43.2`
  (`GAME_EXPECTED_VERSION=0.43.2`) и **падают**, если версия дерева другая: расхождение
  версии выглядит как мистика («obs стал 587», «тип farm исчез»), хотя причина в другом
  дереве. Измерено 2026-09-18 на трёх независимых деревьях (наш клон v0.43.2, свежий
  `main` = `f4b0261` = тег v0.43.2, тег v0.43.1 = `5e903cb`): везде `obs=607`,
  `actions=61`, 224 квеста, цели `kill=106 collect=76 interact=60 gather=4 escort=4 farm=2`,
  793 `.ts` и 0 `.js`. Переезд на новую версию — отдельная задача: обновить пин,
  пересчитать контракты в `tools/verify_facts.ts` (obsSize, словарь действий, типы целей),
  заново измерить эталоны прогонов и подбор порогов, записать всё в `PROGRESS-<дата>.md`.
* ⬜ **A4. Производительность контура.** Один прогон на 150 решений — десятки секунд,
  поэтому подбор на 3 сидах × 34 параметра × 2 значения дорог. Варианты: параллельные
  миры (по одному процессу на сид, с ограничением по памяти — на 2 ГБ OOM уже был),
  более coarse сетка на первом круге, кэш baseline-прогона.

## Порядок, если продолжать сейчас

1. D5c/D5d (зафиксировать сделанное в git — иначе всё живёт только в песочнице).
2. B2 → D6 (смерти и сиды: это то, что мешает и стенду, и живому миру).
3. B1 (квестовый цикл за бриджем) → A3 (переносимость найденных порогов).
4. A1 (живой сервер) — после A3, чтобы не настраивать политику под headless-песочницу.
