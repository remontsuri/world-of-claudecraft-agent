# woof-ts — состояние на 2026-09-18

Порт архитектуры woof-agent (Java) на TypeScript + Node. Мир — upstream `src/sim`
in-process (без браузера, CDP, HTTP-моста и jar). Все игровые факты импортируются
из дерева игры (`game -> /home/user/woc-game`), руками ничего не переписано.

## Действующая модель наблюдения (обновлено после замечания про онлайн-мир)

Смотреть **ONLINE-WORLD.md**: живые сущности (включая NPC) видны в 60 ярдах, как
в RL-obs игры и строже радиуса интереса живой игры (`PLAYER_INTEREST_DROP_RADIUS
= 100`, headless throttle 80); знание «где квестодатель» — отдельный слой карты
`mapPins` из статического контента (`NPCS[*].pos`/`questIds`, `CAMPS`, `pois`).
Прежний режим «NPC видны везде» оставлен как диагностируемая привилегия стенда
(`--npc-view 0`) с предупреждением и пометкой в evidence.

Результат на честной модели (seed 42, warrior): 150 решений → world_steps=4323,
kills=24, deaths=22, **quests_done=2**, первая сдача на шаге 12; 400 решений →
world_steps=6696, kills=31, deaths=26, quests_done=2. Цепочка: q_hub_know_your_numbers
→ q_wolves (8 волков) → q_greyjaw. Детерминизм сохранён (два прогона identical).
Таблица ниже — история прогонов на привилегированной модели, оставлена для сравнения.

## Что уже доказано прогоном

Команда: `node dist/run.mjs --seed 42 --steps N --every --json evidence/...json`

| прогон | шаги | world_steps | kills | deaths | level | quests_done | первая сдача |
|---|---|---|---|---|---|---|---|
| seed 42 | 150 | 3891 | 24 | 13 | 2 | 1 | шаг 91 |
| seed 42 | 150 (повтор) | 3891 | 24 | 13 | 2 | 1 | шаг 91 |
| seed 42 | 400 | 7809 | 41 | 29 | 3 | 1 | шаг 91 |
| seed 42 | 800 | 11580 | 50 | 51 | 4 | 1 | шаг 91 |
| seed 7 | 150 | 3589 | 12 | 80 | 2 | 1 | шаг 97 |

- **Детерминизм подтверждён**: два прогона одного сида дают побайтово одинаковый
  SUMMARY; другой сид даёт другой результат (сид действительно управляет миром).
- **Критерий J8 по убийствам выполнен** (kills=50 за 800 решений), по квестам —
  один настоящий квест пройден по всей цепочке: `ACCEPT:q_wolves` → 8 убийств
  forest_wolf → `TURN_IN:q_wolves` (шаг 90) → `ACCEPT:q_greyjaw`.
- `frozen_guards=0` в последних прогонах: инвариант «любое решение двигает мир»
  соблюдается (см. ниже).

## Шов под живой мир

Целевой мир сопоставим с онлайн-игрой, поэтому in-process `Sim` — только один из
адаптеров: введён `interface World` + `WorldCapabilities` (`src/world/world.ts`),
`SimWorld implements World`, а агент/исполнитель/арбитраж типизированы
интерфейсом. Возможности мира печатаются до прогона и пишутся в evidence
(`worldCapabilities`). Полный аудит «привилегия стенда vs живой мир» и план
адаптеров (NDJSON env server → клиент авторитетного сервера :8787 `/ws`) —
в **ONLINE-WORLD.md**. Регрессия после введения шва побайтовая: seed 42/150
решений даёт тот же SUMMARY (world_steps=3891, kills=24, deaths=13,
quests_done=1, first_turn_in_step=91), `tsc --noEmit` по нашим файлам чист.

## Заявленные политики (объявлены до измерения, пишутся в evidence)

1. **Наблюдение.** Мобы/трупы/объекты — только в `VIEW_RADIUS=60` ярдов, как в
   RL-наблюдении игры (`obs.ts: d < 60`). NPC и их списки квестов — без ограничения
   (`--npc-view 0`, по умолчанию), это аналог карты и журнала квестов игрока;
   строгий режим — `--npc-view 60`. Строка политики печатается до прогона и
   сохраняется в `observationPolicy` evidence.
2. **Квесты.** Типы целей взяты из данных игры (252 цели / 224 квеста):
   `kill=106, collect=76, interact=60, gather=4, escort=4, farm=2`. Берём только
   kill/collect/interact; gather/farm/escort непроходимы (профессии — явный CUT
   upstream, `headless/CLAUDE.md` от 2026-08-29; сопровождения в ACTIONS нет).
   Квест принимается, только если игра выдаст проходимый: какой именно квест
   выдаст NPC, предсказывается правилом из `sim.ts` (talkToNpc: первый доступный
   в порядке `questIds` с совпавшим `giverNpcId` и без `completionEffect`).
   Предсказание подтверждено эмпирически: у Marshal Redbrook после q_wolves игра
   выдала ровно q_greyjaw, как мы и предсказали.
3. **Выбор NPC.** `questPriority`: сначала квесты только на убийства (0), затем
   убийства+сбор (1), interact (2), только сбор (3); при равенстве — ближайший.
   NPC, у которого для нас ничего нет, запоминается в `memory.exhaustedGivers`
   и больше не посещается.
4. **Уровень боя.** `canFightMob`: макс. уровень моба ≤ уровень игрока + 1.
   Old Greyjaw — 4 уровень, 110 HP, скорость 8.5; идти к нему на 2-м уровне
   значит умереть, а не выполнить квест. Если цель квеста не по уровню —
   качаемся на доступных мобах (`grind`), потом возвращаемся.
5. **Знание карты.** Центры лагерей мобов (`CAMPS` из данных игры) используются,
   если мы ни разу не видели нужного моба: это уровень знания карты/текста квеста
   («Old Greyjaw бродит в глухих лесах севернее волчьих троп»), а не глобальное
   зрение — сами мобы по-прежнему видны только в 60 ярдах.
6. **Бой 1×1.** Не начинаем бой, если у цели больше одного соседа в 18 ярдах;
   выходим из боя при HP<55% (или <75%, если в бой втянулась стая).
7. **Инвариант движения мира.** Любое решение обязано сделать хотя бы одно
   каноническое действие; нарушение считается (`frozen_guards` в SUMMARY),
   логируется и исправляется принудительным `noop`. Причина: мёртвый агент без
   действий мира никогда не воскрешался (мир стоял на step=985).

## Факты игры, которые изменили решения

- `RUN_SPEED = 7` ярдов/с у игрока, а мобы быстрее: forest_wolf 8, wild_boar 7.5,
  old_greyjaw 8.5. **Пешком от погони не уйти** — поэтому отрыв делается
  способностями (`hamstring`, `heroic_leap`), а не бегом, и поэтому бой не по
  уровню смертелен. `FLEE_MAX_SPEED = RUN_SPEED*0.65` — это про убегающих мобов,
  не про преследующих.
- Лагерь волков в зоне 1 — 5-6 особей в диске радиусом 26-28.5 ярдов, аггро 10-12:
  одиночных целей почти нет, поэтому «толпа» — нормальное состояние, и мерить
  опасность надо по фактической агрессии (`aggroTargetId == игрок`), а не по
  соседству. Правило «двое в 18 ярдах → беги» превращало прогон в бесконечное
  бегство (418 смертей при 4 убийствах).
- Учебный манекен у двора — hostile с 999999 HP: он не угроза и не цель,
  исключён из «толпы» (`threateningNear`) и из убиваемых целей.
- У воина есть `furious_mending` (самохил) — лечебных заклинаний нет, поэтому
  `heal` = снять автоатаку, отойти от агрессоров, периодически `eat_drink`,
  ждать регена.
- `Sim.countItem(itemId)` — публичный API: по нему отличаем «лут не удался» от
  «лут удался, но труп ещё не пуст» (труп остаётся, если на нём что-то лежит).

## Открытые дефекты (не закрыты)

- **D1. `LOOT_FAILED` (13-24 за прогон).** Гипотеза: полные сумки — в
  `lootCorpse` предмет, который не влез (`canAddItem` false), остаётся на трупе,
  а продавца/`sell_junk` в headless action space нет (CUT upstream). Проверить
  через `sim.bags()/bagCapacity()/inventory()` и решить: либо объявить
  ограничение, либо освобождать сумки каноническими средствами, если они есть.
  Блокирует квесты типа collect, в том числе q_greyjaw (нужен greyjaw_fang).
- **D2. Качели HP.** 390 из 800 решений — `NOT_NOW:hp=…`: агент держится около
  40% HP, потому что в зоне цели квеста постоянно 9-11 мобов и отдых прерывается.
  Нужна боевая политика способностей (сейчас `bestAbility` берёт первую готовую
  по порядку слотов): урон (heroic_strike/execute/overpower/slam), AoE при стае
  (thunder_clap/whirlwind/cleave), защита (raised_guard/defensive_stance),
  самохил (furious_mending) по HP.
- **D3. q_greyjaw не сдан.** Урон по Old Greyjaw есть (`FARM_DMG:old_greyjaw` ×17),
  убийства нет: не хватает DPS и HP. После D2/D1 — повторить прогон на 800+ шагов.
- **D4. Тестов нет.** Нужны vitest: facts (импорт фактов), quest_policy
  (предсказание выдачи квеста сверяется с реальным поведением игры), fsm,
  arbitration (пороги), smoke-прогон агента, детерминизм (два прогона = один
  результат).
- **D6. Хрупкость по сидам.** seed 7: kills=1, deaths=183 при том же коде —
  агент проходит сквозь лагеря (прямой подход), его накрывает стая, пешком уйти
  нельзя (мобы быстрее игрока), а у города арбитраж выбирает `farm` раньше
  `navigate`. Лечение: маршрут в обход лагерей (центры/радиусы из `CAMPS`,
  безопасные точки из `hub`/`pois`), боевая политика способностей (AoE/защита/
  самохил вместо «первой готовой»), приоритет похода к пину квестодателя над
  случайным боем. В живом мире сид не выбирают, поэтому это часть acceptance.
- **D5. Обвязка репозитория.** `tools/setup_game.sh` (npm install + симлинк game
  + build — node_modules стирается между ходами), `tools/check_all.sh`, README
  линии, дата-строка в ROADMAP (woof-ts — активная линия, woof-agent заморожена),
  коммит и push в ветку backup (нужен свежий PAT; прежний засветился в чате —
  отозвать).

## Как повторить

```bash
cd /home/user/repo/woof-ts
npm install                                  # node_modules не переживает смену хода
ln -sfn /home/user/woc-game game             # симлинк на дерево игры
npm run build                                # esbuild -> dist/run.mjs
node dist/run.mjs --seed 42 --steps 400 --every
node dist/run.mjs --seed 42 --steps 400 --npc-view 60   # строгий режим видимости NPC
```

---

## Добавлено 2026-09-18 (вторая половина дня): бридж убивает, контур обучения, перепись правил

Секция добавлена, предыдущие не переписаны.

### 1. Критерий «прогон состоялся» выполнен через живой бридж

Seed 42, воин, `--transport ndjson --steps 120` (мир — headless env-сервер игры в отдельном
процессе):

```
[WoOF] транспорт: ndjson-бридж к dist-env/env_server.cjs
[WoOF] мир: транспорт=ndjson-stdio, … состояние квестов=observed, идентичность сущностей=slot,
       вид сущностей (templateId)=нет, абсолютное HP сущностей=нет
[WoOF] мир: счётчики урона=нет (0, не используется как сигнал), …
[Agent] SUMMARY steps=120 world_steps=5530 kills=2 deaths=0 quests_done=1 first_turn_in_step=6
[WoOF] ПРОГОН СОСТОЯЛСЯ: есть настоящие убийства
```

Регрессия стенда после всех правок политики не изменилась: seed 42, 150 решений →
`4323 / 24 / 22 / 2 / шаг 12` (×2, детерминизм).

### 2. Почему за бриджем раньше было kills=0 (диагноз `--every`, 40 решений)

После `GIVER_EXHAUSTED:hale` агент принимал у cook_marlow профессиональный квест
`q_prof_workorder_kitchens` (не проходим в headless: нет сбора и крафта) и уходил в цикл
`FARM_FAILED`. `pickTarget` работал правильно (фильтр по убиваемости, а не по виду), но
критерий успеха боя требовал **накопленный урон**, которого по проводу нет
(`damageCounters=false`). То есть каждый бой без убийства выглядел провалом, навык вставал
на перерыв, и агент не добивал цель.

### 3. Правки политики (не бриджа)

* `src/skills/executor.ts`: `entityHpFraction(e)` = hp/maxHp — работает в обоих транспортах
  (за бриджем hp уже доля); в бою отслеживается `fracMin` по текущей цели; порядок проверки
  успеха: убийство → прогресс квеста → накопленный урон (**только** при `caps.damageCounters`)
  → **падение наблюдаемой доли HP цели** (`FARM_HP:<цель>:до→после`) → `FARM_FAILED`.
* Метка цели во всех результатах боя — `targetLabel(e)` = `templateId || 'вид_неизвестен'`
  (за бриджем вида нет, и это видно в логе, а не замаскировано).
* `saveMobSpot` только при непустом `templateId`: иначе в память писался мусор
  («пятно вида ''»), который потом влиял на выбор цели.

### 4. Контур самоулучшения построен и продемонстрирован

* `src/learn/objective.ts` — цель объявлена до измерения:
  `score = 100·quests_done + 5·kills − 10·deaths + 25·(level−1) + 20·[first_turn_in≤40] − 0.001·world_steps`;
  `seedSplit` бросает исключение при пересечении train/val.
* `src/learn/tune.ts` — покоординатный спуск по сетке `PARAM_SPECS` (def ± step, ± 2·step,
  в `[min, max]`); каждый кандидат — **отдельный процесс** (`PARAMS` читается при импорте,
  поэтому только через `WOOF_PARAMS=<file>` в окружении ребёнка); кандидат проходит ту же
  строгую валидацию `loadParams`; принятие — только при `trainMean > bestMean + minDelta`;
  val-проверка с громким предупреждением о переобучении.
* Артефакты: `learning/history.jsonl` (append-only), `learning/best.json`,
  `learning/last_report.md`. Сборка: `npm run build` собирает `dist/run.mjs` и `dist/tune.mjs`.

Demo-прогон (не мок): `--steps 60 --train-seeds 42 --val-seeds 44 --groups quest` —
baseline train −71.56 → **принято `levelDeltaMax: 1→0` (train 116.52)**; отклонены
`levelDeltaMax→2`, `giverFailExhaust→2/4`; val −13.95 против базового −14.02.

**Главный вывод:** скор зависит от бюджета решений. Тот же `levelDeltaMax=0` на 150 решениях
даёт `kills=0, world_steps=10684, quests_done=1` — хуже базового. Значит подбор обязан
фиксировать `--steps` (фиксирует и пишет в `best.json`/отчёт), а сравнивать можно только
прогоны одного бюджета. Один val-сид — шум: минимум три.

### 5. Перепись правил, хуков и документов под новый стек

* `../AGENTS.md` — переписан: активная линия `woof-ts`, стек, шов `World` и транспорты,
  таблица порогов, контур обучения, эталоны прогонов, границы, обновлённый каталог
  «правил, которые спасали работу»; Java-правила сохранены в разделе «Замороженные линии»
  (действуют при разморозке). Стоячие правила (ветка `backup` + fast-forward, запрет
  `--force`, протокол убийства процессов, отсутствие молчаливых фолбэков, пороги до замера,
  заморозка архива, предпочтения пользователя) перенесены без изменения смысла; добавлено
  правило про PAT (только env на одну команду, отзыв после пуша).
* `../README.md` — карта репозитория под новую линию: быстрый старт
  (`setup_game.sh` → `check_all.sh` → `run.mjs` → `tune.mjs`), эталоны, границы, лицензии
  (игра — MIT, мы её не модифицируем).
* `BRIDGE.md`, `LEARNING.md`, `README.md` линии — созданы; `../knowledge/woof-ts.md` —
  создан; датированные строки дописаны в `../knowledge/pitfalls.md` (два новых раздела:
  среда/сборка/процессы и наблюдение/логика агента), `../knowledge/principles.md` (§11),
  `../knowledge/verification.md` (§4b — приёмка активной линии), `../knowledge/README.md`
  (индекс).
* `.githooks/pre-commit` — создан заново под стек: секреты в индексе, archive/
  (удаления и переименования запрещены, правки — только append-only в `archive/**/README.md`),
  мусор (`node_modules`, `dist`, `dist-env`, симлинк `game`, `evidence/tuning/`), изменения
  `woof-ts/` без `check_all.sh` (`WOOF_HOOK_FAST=1` — громко печатаемый режим с `SKIP_TUNE=1`).
  Логика проверена на временном git-репозитории: 7 сценариев дали ожидаемый результат
  (секрет — отказ, удаление в archive — отказ, правка архива задним числом — отказ,
  append-only строка — принято, node_modules — отказ, артефакт сборки — отказ, обычный
  файл — принято). На реальной истории `backup` хук не проверялся: в песочнице `.git`
  стирается между ходами — это пункт D5d в `ROADMAP.md`.
* `tools/check_all.sh` — evidence бриджа теперь пишется в отслеживаемую датированную папку
  (`evidence/<дата>/bridge-smoke-seed42.json`) и дополнительно проверяется, что в нём
  объявлены транспорт и ограничения (`transport.kind`, `entityTemplates=false`,
  `questStateApi="observed"`): proof без объявленных возможностей неатрибутируем.
* `.gitignore` += `woof-ts/evidence/tuning/` (черновики подбора не трекаем; итоги — в
  `learning/`).

### 6. Что осталось

См. `ROADMAP.md`: D5c/D5d (коммит и пуш с новым PAT, проверка хука на живой истории),
B1 (полный квестовый цикл за бриджем), B2 (смерти на стенде), D6 (робастность по сидам),
A1 (WS-транспорт к живому серверу `:8787`), A2 (`gather`/`craft`), A3 (переносимость
найденных порогов на бридж), A4 (производительность контура).

### 7. Добавлено тем же вечером: гейт поймал два дефекта, контур обучения ужесточён

Полный `bash tools/check_all.sh` после переписи правил упал на шаге 7 — и это была не
формальность:

1. **Проверка объявления цели не работала.** `grep -qi 'целевая функция (…)'` не находил
   строку `## Целевая функция (объявлена до измерения)`: в локали C `grep -i` не складывает
   байты кириллицы. Исправлено на литеральное `grep -qF` + отдельная проверка наличия
   раздела «Вердикт».
2. **Шаг не проверял страж переобучения.** Добавлена сверка чисел с текстом: если
   `valMean < baselineValMean`, в отчёте обязан быть вердикт «ПЕРЕОБУЧЕНИЕ», иначе —
   «не хуже базового»; `best.json` обязан содержать `objective` и `warnings`, история —
   базовое измерение. Проверено на реальном прогоне шага 7 (`val=-12.678 < база=-12.418`
   → вердикт на месте).

Попутно ужесточён сам контур (правила поиска, не целевая функция — веса не двигались):

* `--min-delta` по умолчанию **1.0** вместо 0: наблюдалось принятие кандидата
  `searchActions: 12 → 10` с приростом **+0.05** (шум). После правки тот же кандидат
  отклонён в том же прогоне.
* `warnings` объявляются до измерения и пишутся в отчёт и `best.json`: один train-сид,
  меньше трёх val-сидов, бюджет < 100 решений.
* Отчёт: раздел «Вердикт» и формулировка «принято шагов N; отличий в итоговом наборе M»
  (прежнее «принято 3» при двух отличиях читалось как расхождение — один параметр
  принимался дважды: `navMaxSteps 80→70→90`).

Повторный подбор с тремя val-сидами (`--steps 60 --train-seeds 42 --val-seeds 43,44,45
--groups quest`, 11 прогонов, 182 с) — текущие артефакты в `learning/`:

| | train (42) | val (43, 44, 45) |
|---|---|---|
| базовые пороги | −71.56 | −3.76 |
| `levelDeltaMax: 1 → 0` (принято) | **116.52** | **72.95** |

Вердикт контура: «на val-сидах набор не хуже базового — можно применять с оговоркой про
размер сплита». Отклонены: `levelDeltaMax→2`, `giverFailExhaust→2`, `giverFailExhaust→4`.

Пре-коммит хук создан заново под стек и проверен на временном git-репозитории (7 сценариев,
см. `../knowledge/woof-ts.md`); на реальной истории `backup` его проверка — пункт D5d.
Evidence бриджа переехал из игнорируемых черновиков в `evidence/<дата>/bridge-smoke-seed42.json`,
и гейт проверяет объявленные в нём возможности транспорта.

### 8. Имя сданного квеста в evidence (найдено чтением собственного артефакта)

`summary.turnIns[0].questId` был `"?"`: прирост счётчика сдач виден на шаге после сдачи,
когда квест уже закрыт и `fsm.questId` пуст. Добавлено `lastKnownQuestId` в
`src/core/agent.ts` (заполняется после каждого `syncFrom`), сверка берёт
`fsm.questId ?? lastKnownQuestId ?? '?'`.

Было → стало (тот же прогон, seed 42, бридж, 120 решений):

```
turnIns: [{"step":6,"worldStep":157,"questId":"?","total":1}]
turnIns: [{"step":6,"worldStep":157,"questId":"q_hub_know_your_numbers","total":1}]
SUMMARY steps=120 world_steps=5530 kills=2 deaths=0 quests_done=1 first_turn_in_step=6   (без изменений)
```

Полный гейт после правки: `ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ` (8 шагов, 62 теста, стенд ×2,
живость порогов, бридж, контур обучения со сверкой стража переобучения).
Также `tools/build_env.sh` печатал `--steps 50` вместо реально запускаемых `--steps 120` —
напечатанное приведено к выполняемому.

### 9. Добавлено ночью: «сборка зелёная, тесты красные» на машине владельца

Симптом: `npm test` → `ReferenceError: exports is not defined in ES module scope`,
при этом `npm run build` собирал `dist/run.mjs` и `dist/tune.mjs` без ошибок.

Корень: в дереве игры `D:\woc-game` оказалось 523 файла `.js` (собранный вывод), хотя
в чистом клоне upstream их **0** при 793 `.ts`. Наши импорты игры без расширения, а
порядок резолвера у esbuild (`.ts` раньше `.js`) и у Vite/vitest (`.js` раньше `.ts`)
противоположный — поэтому сборка брала исходники, а тесты собранный CJS; игра объявляет
`"type": "module"`, и CJS-`exports` в ESM-области даёт ровно эту ошибку.

Доказательства (репродукция на копии дерева, три сценария): рядом `obs.ts`
(`NUM_ACTIONS=2`) и `obs.js` (999) → дефолтный vitest грузит **999**; с
`resolve.extensions` (`.ts` первым) грузит **2**; на чистом дереве — **2** и с дефолтом.
На реальном проекте при грязном дереве `tests/facts.test.ts` с новым конфигом: 9/9 passed.

Сделано:
* `vitest.config.ts`: `resolve.extensions: ['.mts', '.ts', '.tsx', '.mjs', '.js', '.jsx', '.json']`
  с комментарием про разницу с esbuild;
* `tools/verify_facts.ts`: секция C — отказ, если в `src/sim`/`headless` дерева игры есть
  `.js/.cjs/.mjs` (текст ошибки объясняет причину и что делать);
* `.gitignore` + `.githooks/pre-commit`: копия дерева игры (`.game-cjs/`) в репозиторий не
  пускается; `AGENTS.md` §«What to Never Do» п.4 расширен тем же запретом;
* `knowledge/pitfalls.md` и `knowledge/woof-ts.md`: датированные записи.

Костыль `.game-cjs/` (523 переименованных файла) признан неверным путём: `.cjs` остаётся
CJS, `tsc` теряет типы, сверка фактов уходит с upstream на копию. Лечение на машине
владельца — очистить или переклонировать дерево игры (команды выданы отдельно).

### 10. Добавлено: починка окружения на Windows одной командой

Владелец работает на Windows 11, и его исполнитель запутался: диагноз был поставлен как
«игра использует CommonJS», а лечением выбрана копия дерева игры `.game-cjs/` (523 файла
`.js` → `.cjs`) с алиасами в `vitest.config.ts`/`tsconfig.json`. Это неверно по существу
(см. §9): в upstream 0 файлов `.js` при 793 `.ts`, значит `.js` в дереве — собранный вывод,
а алиасы на копию ломают типы и уводят сверку фактов с upstream.

Чтобы исполнителю не пришлось принимать решения, добавлен сценарий
`tools/fix_windows_env.ps1` (+ обёртка `tools/fix_windows_env.bat`) — идемпотентный,
с таблицей итога и кодом возврата:

1. удаляет `.game-cjs/` (копия дерева игры запрещена);
2. предупреждает, если в `tsconfig.json` остались алиасы на копию;
3. диагностирует дерево игры: считает `.js/.cjs/.mjs` в `src\sim` и `headless`;
4. если дерево загрязнено — **по умолчанию не трогает его**, а делает чистый sparse-клон
   рядом (`<GameDir>-clean`) теми же параметрами, что и `tools/setup_game.sh`;
   деструктивная очистка на месте — только с явным `-CleanInPlace` (и сначала сухой
   прогон `git clean -xdn`);
5. создаёт `game` как junction (`mklink /J` — права разработчика не нужны); прежнюю
   ссылку убирает через `cmd /c rmdir`, а не `Remove-Item -Recurse` (последняя на
   junction способна вынести содержимое цели);
6. добавляет в `vitest.config.ts` блок `resolve.extensions` (`.ts` раньше `.js`), если
   его нет;
7. ставит зависимости, собирает, сверяет факты (`dist/verify_facts.mjs`) и гоняет тесты;
8. печатает `Шаг | Статус | Детали` и `ОКРУЖЕНИЕ ГОТОВО` / список провалов, `exit 1` при
   любом провале.

Файл сохранён в UTF-8 **с BOM**: PowerShell 5.1 (штатный в Windows 11) читает `.ps1`
без BOM как ANSI, и кириллица в сообщениях поехала бы. Статически проверен баланс скобок
и закрытие here-string; на живой Windows сценарий не запускался — это пункт D5e в
`ROADMAP.md` (прогнать на машине владельца и записать результат).

### 11. Добавлено: «upstream обновился (obs=587, farm исчез)» — проверка показала старое дерево

Исполнитель на машине владельца сообщил, что дерево игры обновилось upstream: `obs=587`,
тип цели `farm` исчез, и это «не только CJS/ESM». Проверили до всяких правок.

Измерено одним и тем же `tools/verify_facts.ts` на семи независимых деревьях:

| Дерево | Версия | obs | квестов | NPC | мобов | цель `farm` |
|---|---|---|---|---|---|---|
| свежий клон `main` = `f4b0261` (= тег v0.43.2) | 0.43.2 | 607 | 224 | 108 | 250 | есть (2) |
| тег `v0.43.2` | 0.43.2 | 607 | 224 | 108 | 250 | есть (2) |
| тег `v0.43.1` = `5e903cb` | 0.43.1 | 607 | 224 | 108 | 250 | есть (2) |
| тег `v0.43.0` | 0.43.0 | 607 | 224 | 108 | 250 | есть (2) |
| тег `v0.42.0` | 0.42.0 | 607 | 224 | 108 | 245 | есть (2) |
| тег `v0.41.4` | 0.41.4 | **593** | **217** | 102 | 242 | **НЕТ** |
| тег `v0.41.3` | 0.41.3 | **593** | **217** | 102 | 242 | **НЕТ** |

`git ls-remote`: `main` = `f4b0261511ceea49ddd97454710ba0d315f0f76f` = тег `v0.43.2` —
upstream не менялся.

Расшифровка числа 587 арифметикой наблюдения (`src/bridge/obs.ts`, `obsLayout()`):

```
obs = 16 (self) + 96 (2×48 способностей) + 9 (цель) + 30 (6×5 мобов) + 5 (interactable)
      + 2·Q (Q = число квестов) + 3 (paladin)
Q=224 → 607      Q=217 → 593      Q=214 → 587
```

То есть `obs=587` — это дерево с 214 квестами: линия v0.41.x (217 квестов, `farm`
отсутствует) и ещё около трёх квестов не доехало. Механика понятна: `src/sim/data.ts`
тянет **56 модулей из `src/sim/content/**`, где 92 файла**, а квесты цели `farm` живут в
`src/sim/content/zone1.ts`; при неполном checkout или частичной копии теряются ровно так —
минус несколько квестов и минус тип цели. Вывод: **дерево старое и/или неполное**, а не
новый upstream. `git clean -xdf` это не лечит (clean не трогает отслеживаемые файлы) —
нужны `git fetch --tags` и `git checkout v0.43.2`.

Что внесено, чтобы диагноз ставился сам:

* `tools/setup_game.sh`: пин `GAME_REF=v0.43.2` (вместо `main`) и
  `GAME_EXPECTED_VERSION=0.43.2`; два громких отказа — (а) версия дерева не совпадает
  с ожидаемой, (б) в `src/sim`/`headless` есть собранный вывод `.js/.cjs/.mjs`. Оба
  печатают команды приведения дерева к пину.
* `tools/verify_facts.ts`: версия дерева игры печатается и в отчёте, и в провале; текст
  провала объясняет, что расхождение контрактов при другой версии означает «дерево другое»,
  а не «upstream уехал вперёд», и приводит формулу obs (по ней видно, сколько квестов
  не доехало). Прежняя формулировка «тип цели farm исчез — политика квестов устарела»
  оказалась двусмысленной: на старом дереве она читается как дрейф upstream.
* `tools/fix_windows_env.ps1`: параметры `-GameRef` / `-ExpectedVersion` (пин v0.43.2) и
  шаг «3. версия дерева игры» с отказом и командой приведения.
* `ROADMAP.md`: пункт A5 — осознанный переезд на новую версию игры как отдельная задача
  (пересчёт контрактов `verify_facts`, повторные измерения эталонов и подбора порогов).
* `knowledge/woof-ts.md`: датированная запись с таблицей семи измерений.

Проверки (негативные и позитивные, все реальные прогоны):

| Сценарий | Ожидание | Факт |
|---|---|---|
| дерево v0.41.4 | отказ с указанием версии | `ПРОВАЛ: … версия 0.41.4, а эталоны линии измерены на 0.43.2` + команды `fetch --tags`/`checkout`/`clean`; `exit=1` |
| то же дерево, `verify_facts` | отказ с объяснением | `дерево игры: версия 0.41.4 (контракты измерены на 0.43.2)`, `obsSize=593, ожидали 607`, `тип цели "farm" исчез`, блок «ПЕРВОЕ, что проверить» с формулой obs; `exit=1` |
| дерево v0.43.2 с подложенным `src/sim/obs.js` | отказ | `ПРОВАЛ: в дереве игры есть собранный вывод … в upstream там только .ts`; `exit=1` |
| чистый клон v0.43.2 | принято | `[setup] дерево игры: версия 0.43.2 (совпадает с ожидаемой), ref=v0.43.2`; `сверка фактов: OK` |
| полный гейт | зелёный | `ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ` (62 теста, стенд `4323/24/22/2/12` ×2, бридж `5530/2/0/1/6`, контур обучения) |


## Добавлено 2026-09-18 (вечер): провод живого мира — A1.0 и A1.1

Задача владельца: «игру запускаем в браузере, бридж передаёт мир агенту». Решение по фактам
upstream: браузер — для человека, агент подключается **отдельным WS-клиентом** к
авторитетному серверу `:8787` со **своим** персонажем. CDP 9222 не используется: это
архитектура замороженной Java-линии (`woof-agent/`, мост :8791, MCP :8792).

Факт, который определил схему: `server/linkdead.ts` → `planJoin` при занятом персонаже
отвечает `reject 'character already in world'`; повторный вход — только явным takeover
(`POST /api/characters/:id/takeover`, `src/net/online.ts:807`), а takeover выбрасывает
сеанс человека. Значит «агент играет моим героем, я смотрю» невозможно технически:
смотреть будет нечем.

### Что сделано

| Артефакт | Содержимое |
|---|---|
| `src/bridge/ws_protocol.ts` (230 строк) | кадр рукопожатия из констант upstream, кадр команды `{t:'cmd', cmd, …}`, запрет dispatch-only токенов, разбор кадра сервера, `liveWorldCapabilities()` + `unmeasuredCapabilityNotes()` |
| `src/bridge/ws_socket.ts` (165 строк) | `SocketLike`, адаптеры browser-style и `ws`, `resolveSocketFactory()` с порядком injected → global → ws → ПРОВАЛ |
| `tests/ws_live.test.ts` | 21 проверка: 20 прошли, 1 пропущена ВСЛУХ (см. ниже) |
| `tools/run_world.sh` | подъём мира: полный чекаут + пин версии, `.env` и пароль Postgres, `compose up -d --build postgres game`, готовность по `/api/status`, флаги `--clone/--no-build/--link-game/--print-only/--logs/--down` |
| `package.json` | `ws@^8` + `@types/ws@^8`, `--external:ws` в сборке |
| документы | `AGENTS.md` (зависимость и причина), `ROADMAP.md` (A1.0–A1.6), `ONLINE-WORLD.md` (факты сервера и протокол), `knowledge/woof-ts.md` |

### Как проверено

* `tsc --noEmit` чисто: `ws_protocol.ts` импортирует `game/src/world_api` и компилируется
  **против лёгкого дерева фактов** — `src/net/**` для слоя протокола не нужен.
* `tests/ws_live.test.ts`: 20 passed, 1 skipped. Пропущенная — привязка `movementWire=2`
  к исходнику `game/src/net/world_auth_message.ts`: в лёгком дереве файла нет, тест
  печатает `[ws_live] пропуск привязки к upstream-помощнику … значение movementWire=2
  в этом прогоне НЕ сверено`. На машине с полным чекаутом (`--link-game`) она выполнится.
* `tools/run_world.sh` — **исполнением с заглушкой `docker` в PATH** (настоящего docker
  в песочнице нет): `--print-only` печатает все команды; чужая версия дерева мира
  (0.41.4) → ПРОВАЛ с командами `fetch --tags && checkout v0.43.2`, код 1; `.env`
  создаётся из `.env.example`, `POSTGRES_PASSWORD` генерируется (64 hex), в журнал не
  попадает, `DATABASE_URL` согласован; compose вызывается как `up -d --build postgres game`
  (discord-bot и mediawiki/mariadb не поднимаем — им нужны свои токены и БД); готовность
  не достигнута → журнал `compose logs --tail 80 game` и ПРОВАЛ; `ALLOW_DEV_COMMANDS=1`
  в `.env` → предупреждение о привилегированном прогоне.
* `bash -n` чисто. shellcheck в песочнице не установлен — это объявлено, а не пропущено молча.

### Что НЕ проверено (объявлено честно)

* `docker compose up` с настоящим docker и Postgres — в песочнице docker отсутствует.
  Проверка на машине владельца: `bash tools/run_world.sh --clone` (нужны Docker Desktop
  и несколько ГБ диска), затем `http://localhost:8787` в браузере.
* Живой WS-сеанс (A1.2/A1.3): REST-вход агента, первый кадр, снапшоты, задержка.
  Всё, что объявлено в `liveWorldCapabilities()` без измерения, объявлено консервативно;
  `latencyMs` — обязательный параметр, дефолта нет.
* Полный чекаут v0.43.2 совместим с гейтом (измерено до удаления клона): `tsc` чисто,
  `сверка фактов: OK`, obs=607, 224 квеста, цели `kill=106 collect=76 interact=60
  gather=4 escort=4 farm=2`. Клон удалён из рабочего места: полное дерево с `docs/`
  заняло 4.0 ГБ при пределе снимка ~128 МБ.

### Исправлено попутно

* Комментарий в `src/world/sim_world.ts` утверждал, что онлайн есть «груп-лут (need/greed
  roll)». В таблице `COMMAND_NAMES` v0.43.2 токенов ролов не нашлось, а фасет
  `IWorldLoot.submitLootRoll` есть. Объявление `partyLootRolls` теперь выводится из
  таблицы (`lootRollTokens()`), а не берётся из ожидания: факт важнее комментария.


## Добавлено 2026-09-18 (ночь): обзор чужих ботов и контракт провода из upstream

Запрос владельца: «нужен агент, который сам играет; поищи в интернете, может кто-то делал
бота». Результат — `UPSTREAM-BOTS.md` (обзор с источниками).

* **Сторонних агентов нет.** Поиск выдал только форки игрового репозитория (PoorInfz,
  demon820308, vagners, lionfart, AccompliceNZ, lumon-bdip, dandpb, SuinegXY, Botro-Plays,
  EPOSigma) и общие материалы по Stable-Baselines3/HuggingFace. Единственный сторонний
  агентский проект — `remontsuri/world-of-claudecraft-agent` (Java + CDP), уже
  отревьюирован и архивирован этим репозиторием.
* **Боты есть в самой игре и они играют**: `scripts/crypt_raid.mjs` — пять ботов
  (warrior, paladin, priest, mage, hunter; focus fire + два хилера) зачищают Hollow Crypt
  примерно за пять минут через настоящий сервер; `mp_integration.mjs` — register/login/
  character CRUD/два клиента видят друг друга/движение/бой/чат/персистентность;
  `social_e2e.mjs` — торговля и дуэль; `arena_visual.mjs` — ranked 1v1; `squad_visual.mjs`
  — 5v5 CTF. Браузерные скрипты (`smoke_browser.mjs`, `smoke_mage.mjs`, `smoke_rogue.mjs`,
  `visual_tour.mjs`) работают через `puppeteer-core` и нужны для UI и скриншотов.
* **Играющие боты upstream — проводные**, не браузерные: они говорят с `npm run server`
  напрямую (`WebSocket` из пакета `ws`, кадр `worldAuthMessage(token, characterId)`,
  команды `{t:'cmd', …}`, движение `{t:'input', mi, facing}`). Это подтверждает выбранную
  схему A и даёт готовый контракт вместо догадок (`UPSTREAM-BOTS.md` §3).
* **Живой мир богаче headless-наблюдения** — и это видно из кода upstream-ботов:
  абсолютные координаты (`self.x/z`), абсолютное HP (`hp`/`mhp`), templateId (`tid`),
  стабильные id сущностей (`ents` + `keep`), серверный журнал квестов (`qlog`/`qdone`),
  инвентарь (`inv`), кулдауны (`cds`), группа (`party.members[]`). План измерения A1.6
  составлен по этим полям; объявим только то, что увидим в кадре.
* **Закрыта дыра, описанная самим upstream**: чат и каждый `/dev …`-чит едут КОМАНДОЙ
  (`{t:'cmd', cmd:'chat', text}`), а кадр `{t:'chat'}` верхнего уровня сервер молча
  выбрасывает — «leaving the script believing its bots were levelled, geared or god-moded»
  (`scripts/lib/world_auth.mjs`). `buildCommandFrame` теперь падает на `/dev` в тексте
  чата (тест добавлен, 22 проверки в `tests/ws_live.test.ts`: 21 прошла, 1 пропуск вслух).
* **Проверено**: `tsc --noEmit` чисто, `tests/ws_live.test.ts` 21/21 при одном честном
  пропуске (привязка `movementWire` к полному дереву). Полный гейт — ниже по тексту.


## Добавлено 2026-09-18 (второй круг поиска): чужой агент уже играет вживую

* **ClaudeCraft Arena** — самоулучшающийся харнесс авторов игры (**форк Hermes**), четыре
  фронтирные модели (Claude, ChatGPT, Grok, Kimi K3) на одном живом сервере, каждая —
  VTuber с голосом ElevenLabs; агенты выполняют квесты, торгуют, воюют и **переписывают
  собственные стратегии в реальном времени**; победителя определяет живой XP-лидерборд.
  Трансляция: twitch.tv/claudeplaysclaudecraft. Подробности и источники — `UPSTREAM-BOTS.md` §8.
* **Их способ обучения совпадает с нашим по существу**: навыки как code policies, оценка
  в сравнении с другими агентами в production, работающее сохраняется, неработающее
  переписывается, без fine-tuning и без человека. Отличия → новые задачи A1.7 (XP/лидерборд
  как метрика), A7 (самоулучшение в живом мире), A8 (два агента на одном мире).
* **Ранний агент авторов** (тред 2026-06-26): Claude Code (Sonnet) как мозг, состояние
  игры + картинка на 1 FPS, бой вынесен в скрипты-«рефлексы», написанные Opus и вызываемые
  как инструменты; начинали как QA-агент; игра изначально задумывалась как RL-окружение.
* **На официальном сервере боты банятся** («bot detection/banning … don't welcome AI's yet
  into the main server») → наш агент играет на своём мире (docker compose, :8787),
  что и заложено в схеме A.
* **Харнесс Арены не опубликован** (код не выложен, лаборатории зовут в ЛС). Открыт только
  сам Hermes: github.com/NousResearch/hermes-agent. Брать неоткуда — берём архитектуру.
* **Исправление нашей же ошибки:** `remontsuri/world-of-claudecraft-agent` — не сторонний
  проект, а наш собственный бэкап (описание «private backup», ветка `backup`). В первом
  круге обзора он был назван «единственным сторонним агентским проектом» — неверно.
  По данным GitHub API на 2026-09-18: 26 репозиториев по запросу, все кроме этого — форки
  игры; игра: 2258 звёзд, 716 форков, MIT, размер ≈9.9 ГБ.
