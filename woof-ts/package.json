{
  "name": "woof-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Автономный агент World of Claudecraft на TypeScript: мир — headless src/sim upstream, действия — канон ACTIONS",
  "scripts": {
    "setup": "bash tools/setup_game.sh",
    "build": "esbuild src/run.ts --bundle --platform=node --format=esm --outfile=dist/run.mjs --log-level=warning --external:ws && esbuild src/learn/tune.ts --bundle --platform=node --format=esm --outfile=dist/tune.mjs --log-level=warning --external:ws && esbuild tools/cdp_probe.ts --bundle --platform=node --format=esm --outfile=dist/cdp_probe.mjs --log-level=warning --external:ws && esbuild tools/run_offline.ts --bundle --platform=node --format=esm --outfile=dist/run_offline.mjs --log-level=warning --external:ws",
    "probe": "node dist/cdp_probe.mjs",
    "offline": "node dist/run_offline.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "bash tools/check_all.sh",
    "tune": "node dist/tune.mjs"
  },
  "dependencies": {
    "@types/node": "^26.6.1",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "ws": "^8",
    "@types/ws": "^8"
  }
}
