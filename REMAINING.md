# Что осталось до «муха полноценно играет в World of Claudecraft»

Срез на 2026-09-15. Критерий «полноценно играет» — не reward и не бенчмарк, а
**прогресс внутри игры**, измеренный её же величинами: уровень/xp, киллы, закрытые квесты,
пройденный путь. Пороги зашиты в `fly-woc/progress_eval.py` (`ACCEPTANCE`) и объявлены
заранее, чтобы их нельзя было подкрутить после замера.

## 1. Вехи

| Веха | Критерий (полный эпизод 8000 шагов = 33 игровые минуты) | Сейчас | Что осталось |
|---|---|---|---|
| **M0** «живёт» | выживает эпизод, 1 квест | ✅ **взята** — 39.47 reward, 1 квест, шаг 724 | ничего |
| **M1** «первый прогресс» | уровень ≥ 2 (400 xp) | ❌ уровень 1, 60 xp | учить с v2-энкодером и без каст-спама (прогон идёт) |
| **M2** «боевой цикл» | ≥ 10 киллов, ≤ 1 смерть | ❌ 0 киллов | та же обкатка; `interact_prox` уже во входах v2 |
| **M3** «квестовый цикл» | ≥ 5 закрытых квестов за эпизод | ❌ 1 квест и стоп | 10–30 млн шагов, γ 0.999, роллаут 256–512 |
| **M4** «выход в мир» | уровень ≥ 6 (Mirefen/6–13), устойчивый маршрут | ❌ | оракул квестов + новые каналы + иерархия «цель → контроллер» |
| **M5** «до капа» | уровень 20 (167 200 xp) | ❌ | масштаб MMO-гринда; цель-ориентир, не обещание |

Приёмка M1+M2+M3 и есть «играет»: муха закрывает квесты, дерётся и получает уровни в
офлайн-режиме. M4–M5 — «играет долго и по миру», это уже про масштаб, а не про сам факт игры.

## 1a. Замер, который объясняет, почему M3 не берётся сам

Прогон v2 + маска + γ0.999, полный эпизод 8000 шагов (33 игровые минуты), 25+ апдейтов:

| СЛОЙ | ОЖИДАЛОСЬ | ФАКТ | СТАТУС |
|---|---|---|---|
| киллы появляются | боевой цикл сам | 2 и 4 килла за эпизод | ок |
| квесты | цепочка стартует | **0 квестов** в обоих эпизодах | провал |
| сигнал до цели | obs ведёт к цели | obs видит цель только в 60 юнитах, первый NPC — в **756 юнитах** | причина |

Вывод: дело не в обучении, а в отсутствии сигнала — квестовая цепочка за пределами
видимости агента. Лечится наградой за сокращение дистанции (`--oracle-shaping`), это и
запущено вместо чистого прогона; чистый сохранён как контроль
(`outputs/params_fly_m3_noshaping_ckpt.pt`).

## 2. Что осталось в коде

| Задача | Зачем | Оценка |
|---|---|---|
| **Обкатка M1/M2 на v2-энкодере + маске GCD** | 71.6 % шагов старой политики — пустые касты; маска снимает шум, v2 даёт пеленги и пеленг интерактива | запущено: 900 апдейтов × 128 × 2 env, ~25 мин в песочнице |
| `progress_eval.py --mask-abilities` | замер политики, обученной с маской, без маски — нечестный замер (уже добавлено) | ✅ сделано |
| γ 0.99 → **0.999** и роллаут 128 → 256–512 | квестовый цикл длиннее 1200 шагов: при γ=0.99 награда за «дойти и сдать» почти не долетает до решений | ✅ флаги есть (`--gamma/--lam/--ckpt-every`), прогон идёт |
| Печать эпизода по факту закрытия, а не по счётчику апдейтов | «0 eps» в консоли пугает, хотя в JSON всё пишется | ✅ `[ep]` печатается на закрытии |
| **Оракул квестов** | координаты целей лежат в самом игровом `src/sim/quest_targets.ts`: без них политика не знает, куда идти, а exploration'ом 224 квеста не перебрать | ✅ извлечён: `quest_oracle.py` + `data/quest_oracle.json` (224 квеста, 215/252 целей с координатами) |
| Новые входные каналы (+ пересборка `circuit.json` с большим `CHANNEL_TYPES`) | «цель далеко/близко/справа» — сейчас таких входов нет | инженерия + пересборка схемы (до этого работает шейпинг) |
| Иерархия «цель → контроллер» | верхний уровень выбирает квест, нижний доводит до цели | инженерия |
| Циклы: триада Брайана (walk/stand/clean) на мухе | `fly-escape` из списка показывает, что это делают, у нас нет | отдельная ветка работ |

## 3. Что осталось в доказательствах (честность)

| Задача | Почему нужна |
|---|---|
| ≥ 3 сида обучения на каждое утверждение | сейчас один training seed — для «работает» хватает, для «стабильно работает» нет |
| **Rewired / matched-контроль** | биологический вывод «топология важна» нельзя делать без схемы той же плотности с перепутанными связями; сейчас в README это помечено TODO, и мы его не заявляем |
| LIF-динамика вместо leaky-tanh | упрощение; для LIF нужен порт `brain.py` из fly-craftax + Brian2-оракул |
| Полный граф (211 577 клеток вместо 8 835) | нужна машина с RAM/GPU; на нашей схеме выводы ограничены подмножеством |
| Traced-only edges | часть входов фоторецепторов/LC теряется на уровне датасета |

## 4. Что осталось на твоей машине

Пункты 5 и 6 чек-листа (`VERIFICATION.md` §6) требуют живого окружения, в песочнице их не закрыть:

- engagement-награды в живом прогоне (не офлайн);
- end-to-end прогон «браузер → Play Offline → муха играет».

Там же: коллеге обновить `fly-woc/env_robust.py` + `train.py` (коммит `90f76f2`) и перечитать
`ENV_ROBUST.md` — после этого `--envs 8` не убивает прогон, а причина краха видна в логе.

## 5. Калибровка по awesome-fly: где мы находимся

В списке 16 игровых проектов. Ни один не показывает полноценную свободную игру — общий
паттерн: замороженная схема + обученный readout на **одной узкой задаче**.

| Проект | Задача | Чем ограничен |
|---|---|---|
| Fly Dino (cobanov) | Chromium Dino | фиксированные 80 нейронов, readout на 243 параметра, CEM |
| FlyPong, Fly Chess Lab, Fly64 | Pong / шахматы / Mario 64 | подграф или необученный readout |
| Doomfly / FlyDoom | ViZDoom | обученное выживание не показано (в списке прямо сказано) |
| FLYT3 | крестики-нолики, fire-watch | полный граф, но только readout; свой негативный контроль: MLP 85.7 % против 60.8 % |
| fly-craftax | Craftax | PPO-обучение DN-readout — ближайший по методу, статус «research prototype» |
| **наш проект** | **настоящая MMO: квесты, уровни, бой** | M0/M1 |

Уникальность, если M3 возьмём: не «пройти одну мини-игру», а жить в мире с прогрессией —
квест-цикл, экипировка, уровень до 20; и мы единственные, кто мерит прогресс величинами
самой игры, а не reward'ом. Офлайн-режим = тот же `src/sim`, что и в RL-окружении, поэтому
RL-прогресс и «игра в браузере» — одно и то же, без отдельного порта.

Что до попадания в этот список: с M0/M1 рано — в нём перечислены проекты с задокументированным
результатом. Точка входа — после M3 (квестовый цикл), тогда есть что показывать. PR в
awesome-fly — одна из последних не-технических задач.

## 6. Итог одной строкой

Осталось не «доделать инфраструктуру» — она закрыта (надёжный env, v2-энкодер, метрики
прогресса, офлайн-ранбук, краш-тест). Осталось **выучить политику**: M1/M2 — часы счёта,
M3 — сутки счёта и γ 0.999, M4/M5 — оракул квестов, новые каналы и иерархия. Пока взята M0.
